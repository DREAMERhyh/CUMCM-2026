# AI 使用记录：3-opt2 时间统计

## 输入

```text
读取output/sim/3-opt2/中的5个日志，按照上述格式给出结果
```

## 运行结果

读取了 `solution/output/sim/3-opt2/` 的 5 个 Q3 JSONL。沿用上一任务口径，将一个成功清除的信号源视为一个点；单点虚拟时间为最终累计 `virtual_time_s` 除以成功清除数，单点真实时间为退出记录 `elapsed_real_s` 除以成功清除数。

5 次运行共成功清除 59 个信号源。运行等权平均为 433.716128 虚拟秒/点、15.680428 真实秒/点；按全部成功点合并加权为 427.993775 虚拟秒/点、16.115729 真实秒/点。单次运行单点虚拟时间的样本标准差为 65.777152 秒，真实时间样本标准差为 4.718867 秒；失败清除率为 15.714%，测量动作数/成功点为 9.1525。

与当前目录中的前两组 5 次实验按等权均值比较：相对 legacy，3-opt2 的单点虚拟时间降低 7.009%，真实时间增加 69.288%；相对 new，虚拟时间降低 6.542%，真实时间增加 76.201%。5 个日志均为 HTTP 200、`accepted=true`、最终动作 `exit`，且累计虚拟时间单调不减；`elapsed_real_s` 与服务器时间戳跨度的最大差值仅 0.022 秒。
