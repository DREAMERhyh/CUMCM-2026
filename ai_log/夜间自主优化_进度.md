# 夜间自主优化 · 进度状态文件

> 维护人：Claude（夜间自主任务，2026-09-12 深夜）。若会话中断，新会话先读本文件，
> 从"下一项"继续。所有证据路径相对 `solution/` 根。

## 顶层状态

- 布局基线：**pure_ring8 已锁定**（8 局对账：局4-8 连续一致；口径 cleared==官方 且 c+a==20）。
- 基线配置（晨间锁定）：`Q3Policy(max_refinements=2, fim_cpu_time_limit_s=6)` + `max_actions 8000`（临时生效，待晨间官方验证）。
- 实验环境：simlite（`src/simlite/`）；官方模拟器今夜不可用。
- 门1 规则：同池成对对比，a)cleared 逐局不降 b)c+a==20 c)fallback/clear失败不升 d)动作数≤上限且≥30%裕量 e)虚拟时间改进局>恶化局且中位>0 f)全量测试绿+禁改区零触碰。逐项晋级，一次翻一项。

## 阶段完成记录

### 阶段0：吞吐自校准（完成）
- 环境：20 核，workers=18（留 2 核）。并行吞吐实测 **8.4 局/分钟**（10 局并行 71.2s）。
- 基线（pure_ring8 默认配置，simlite）：10 局全清除（cleared==源数），对账全 OK；
  动作数 282-884（中位 587，最坏局为 8000 上限的 11.1%）→ **裕量充足**（门1 d 条基线通过）。
- 单局本地 JSONL 估算 ≤345KB（884 动作）；8000 动作约 3.2MB 是我们本地日志不受官方
  2MB 加密日志上限约束（官方日志由模拟器生成），且现实耗时 ~8 分钟在 20 分钟窗口内。
- 样本量决策：筛选实验 **120/臂**（约 30 分钟/项）；晋级确认 **300/臂**（约 75 分钟/项）
  仅用于关键对比（阶段1/阶段2）；其余阶段 120/臂；达不到的项目在报告中说明实际达成量。
- 关键观察：simlite 基线下清除率已是 100%（满分）→ 门1 a) 条的实际语义是
  "清除数逐局不得下降+逐局解释"，主要判别维度是虚拟时间与 fallback/clear 成本（e、c 条）。
- 证据：`tests/simlite/exp0_calibration.json`（row 级）。

### 阶段1：E1 基建 + ring7 vs pure_ring8 对比（完成，300 局）
- 证据：`tests/simlite/analysis/exp1_ring7_vs_pure8.json`（墙钟 3371.6s，300 同池）。
- 结果（A=ring7 旧臂，B=pure_ring8）：总虚拟时间 B 更优 160/持平 0/更差 140 局，
  中位差 **-121.4s**；扫描段 **300/300 全部更优，中位 -520.3s**（离线推算 460s 兑现
  且略超，因已发现频道跳过另计）；resolve 段 B 中位 +387s、清除动作段 +484s、
  fallback 中位 +75.5 次（179 局升高）——来源是 pure8 第一观测位置不同 →
  strip 保底网格原点不同 → fallback 起点移动与命中次序的第二阶差异；cleared
  300 局全等（无覆盖问题）。d 条最大动作 1207 仍远小于 8000。
- **裁定：pure_ring8 维持锁定不变**（门1 本项不适用；论文数据与 460s 推算兑现）。

### 阶段2：A1+A6 排除圆修剪（实验完成 300 局 → 决策：不翻默认）
- 证据：`tests/simlite/analysis/exp2_pruning.json`（墙钟 3385s，300 同池）。
- 结果：**两臂 300/300 局完全一致（逐局所有指标零差异）**——物理原因：Q3 全向
  模型下 refine 测点恒在保证接收域（≤R_min=1000 保守内近似）内，永不会
  no_signal，排除圆从不触发。门1 e 条（平局）不满足 → 按规则保持默认 False。
- 决策：**A1 不翻默认**；保留代码/单测作为防御性能力（多边形近似边界情形、
  未来 Q4 定向场景扩展点），报告注明"行为中性、零风险、零收益"。

### 阶段3：A2+A5 直径目标（实验完成 40 局 → 决策：不翻默认，转队列）
- 证据：`tests/simlite/analysis/exp3_diameter.json`（墙钟 3232s，40 同池；120 局版
  因成本 120 分钟未竟而缩减）。
- 结果：总虚拟时间 B 更优 30/更差 10、中位 **-576.2s**；清除动作段 -719s；
  fallback 中位 -101.5 次（33/40 局下降）；动作数 -100；cleared 40 局全等。
  门1：e 通过、c 失败（4 局 fallback 上升，含 +804 一局）。
- 决策：**A2 不翻默认**。理由（报告将列全）：(1) 现实可行性硬伤——单次直径
  规划 90-180s（评估含 24 边半平面交重建），16 源 × 2 refine 约 48 分钟 >
  20 分钟现实窗口；(2) 门1 c 条个别局上升；(3) 实验吞吐极低（40 局 54 分钟）。
  队列项：先优化评估成本（半平面缓存/简化目标）再重试。
- exp3b（新目标下深度实验）因 A2 不上线而意义有限 → 转队列。

### 阶段4：A3 最优停止（120 局筛选通过 → 300 局确认运行中：任务 bvxtyvudo）
- 证据（筛选）：`tests/simlite/analysis/exp4_optimal_stop.json`（120 同池）。
- 筛选结果：总虚拟时间 **120/120 全优**、中位差 **-3461.1s（-33%）**；清除动作段
  -3329s；fallback 中位 514.5→13.5（-440.5 次）；动作数 638.5→144；每源测量
  4.54→3.93；**最坏恶化 0.0（无任何恶化局）**；cleared 全等、c+a 全 20。
  门1 六条全过（overall_pass=True）。
- 机制备注：guess_clear 仅覆盖 MEC 半径 (19.9,40] 窗口且每源一次，miss 后转既有
  refine（保底语句不变，未触碰 certificate/fallback 保险丝）；实证下该窗口
  fallback 链几乎消除（基线的大量 fallback 正来自该窗口源的 refine 不收敛）。
- 晋级流程：120 筛选 → 300 局确认（新 seed 池）→ 全量回归 147/147 通过 →
  已翻默认（决策日志已登记，回滚命令见决策表）。

### 阶段5：B2 批量解耦（120 局筛选过 → 300 局确认运行中：任务 bs6fff1fb）
- 证据（筛选）：`tests/simlite/analysis/exp5_batch.json`（120 同池，双臂均关 A3）。
- 筛选结果：总虚拟时间 **120/120 全优**、中位 -3258.2s（-30%）；**fallback 中位
  528→0（完全消除）**；清除动作段 118/120 优；动作数 -428（647→189）；cleared
  全等、c+a 全 20；max 动作 543。门1 六条全过。
- 机制：批量 locate（每源 refine 至 certificate）→ TSP 全局排序清除；certificate
  源 clear 必成（radius≤19.9⇒中心距源≤19.9<20），交错的大量 fallback 来自
  certified 失败源链，批量流程把失败源转 strip 队列集中处理。
- 风险权衡（报告将列）：批量在 locate 完成前无 cleared（现实中断最坏零清除），
  缓解：locate 虚拟 ~3000s ≈ 现实 3-5 分钟 << 20 分钟窗口；安全退出机制保留。
- 晋级动作（确认后执行）：sim/cli.py 与 run_drill.py 的 Q3 分支默认构建
  Q3BatchPolicy（一行切换，回滚一行）；Q4 不受影响（保持交错）。
- `q3/policy.py`：SourceTrack.exclusion_circles、use_no_signal_pruning=False、
  refine 期 no_signal 才追加排除圆 B(点,1000)；扫描期 no_signal 仍只用于 absent。
- `q2/planner.py`：score_candidates 剔除排除圆内代表点（A6 可行代表点最坏距离，
  全被排除时回退全场景）。
- 单测 tests/q3/test_pruning.py 8 项全绿；实验脚本 exp2_pruning.py（120+ 局）排队。

### 阶段3：A2+A5 直径目标（代码完成、冒烟通过；实验排队）
- `src/q2/continuous_diameter.py`：basin-hopping（16 确定性伪随机重启 + 局地模式
  搜索），最坏后验直径目标，A6 场景过滤，anytime 兜底；Q2Config.continuous_objective
  = "fim"（默认）| "diameter"；plan_measurement 字段归一化接入；Q3Policy 透传。
- 冒烟：plan_second_point(continuous_objective="diameter") ok（含 CPU 竞争下 anytime）。
- 实验：exp3_diameter.py（fim vs diameter 门1）；exp4_depth_diameter.py（新目标下
  重开深度实验 120/600/3000，端到端总虚拟时间口径——此前"+0.02% 关闭"仅对 FIM 目标有效）。

### 阶段4：A3 最优停止（代码完成；单测补写中）
- use_optimal_stop=False + guess_clear_threshold_m=40：MEC 半径 ∈(19.9,40] 时直接
  猜中心点 clear（mode=guess_clear，每源一次，失败后转既有 refine/fallback）。
  最坏情形成本说明：miss 最坏只多 3s 虚拟/源，却省一次规划的墙钟（6s+ 现实），
  对 20 分钟窗口有利；不假设命中概率。

### 阶段5：B2 批量解耦（代码+测试完成、冒烟通过；实验排队）
- `src/q3/batch_policy.py`（Q3BatchPolicy：locate 全部源 → Held-Karp 精确 TSP
  n<=16 排序清除；无证书源队列自动补 strip 保底）。held_karp_tsp 与暴力解对照
  n=1..8 全等。
- 冒烟（同 seed）：批量 135 动作/5866s vs 交错 595 动作/8797s（巨大差异待同池验证）。
- 测试 tests/q3/test_batch_policy.py（TSP 暴力对照 + 2 局端到端，fim 2s 加速）。

### 阶段7：E3 参数敏感性（完成 60/臂 × 5）
- 证据：`tests/simlite/analysis/exp7_sensitivity.json`。
- 结果（全部同池 vs 基线，均含 A3 新默认）：error_deg=0.5）+84s（防御路径代价，
  **低于真实界即不安全**）；error_deg=1.5）+1365s（过保守）；max_refinements=0
  +5646s；=1 +653s；**=4 −505s（32 优/7 差/21 平）但门1 c 条失败**（部分局
  clear 尝试上升）。清除下降局全部为 0。
- 裁定：维持 error_deg=1.005 与 max_refinements=2 默认；r=4 方向性改进转队列
  （扩大样本复验 c 条结构）。安全边界结论：error_deg 不允许低于真实误差界。

### 收尾：最终组合验证（完成，150 局门1 全过）
- 证据：`tests/simlite/analysis/exp_final.json`：夜间最终（批量+A3）vs 晨间
  基线（交错无 A3）：总虚拟 149/150 优、中位 -3151.8s（-29%）、最坏 1 局
  恶化 526.5s；fallback 507.5→0、动作数 637→192；cleared 全等、c+a 全 20。
- 全量 unittest 147/147 通过；禁改区零触碰；无 Git 操作。
- 最终报告：`docs/夜间自主优化报告_20260912.md`（交付消息完整展示）。
- **夜间任务完成，等待晨间反馈（官方演练对账）。**

### 阶段6/8（优先级评估）
- 阶段6（C2 扫描变体/D1 动态调度）：依赖 A3 成本模型结果；代码量中等。
  计划：若阶段2/3/4/5 的实验与晋级处理顺利则实现代码并跑 60 局筛选实验；
  否则转队列（完整计划见正文）。
- 阶段7（参数敏感性）：脚本已就绪（exp7_sensitivity.py：error_deg 0.5/1.005/1.5、
  max_refinements 0/1/2/4 网格 vs 基线），排队。
- 阶段8（论文层）：A4/C1/C3/E2 小节在最终报告中以"草稿小节"形式给出
  （数据来自各实验文件），不改运行时代码。

## 决策日志（翻默认的项）

| 项 | 证据 | 收益 | 回滚命令 | 状态 |
|---|---|---|---|---|
| A3 最优停止（use_optimal_stop=True） | tests/simlite/analysis/exp4_optimal_stop.json（120 筛选 + 300 确认，双池门1 全过） | 总虚拟时间中位 -3307s（-31%）、300/300 局更优、最坏恶化 0.0、fallback -421 次、动作 -421 | `q3/policy.py` 中 `use_optimal_stop=True` 改回 `False`（或 Q3Policy(use_optimal_stop=False)） | ✅ 已翻默认（2026-09-12 夜间），全量 147 测试绿 |
| B2 批量解耦（Q3 入口默认 Q3BatchPolicy） | tests/simlite/analysis/exp5_batch.json（120 筛选 + 300 确认，双臂关 A3 单变量） | 总虚拟时间中位 -3062s（-29%）、fallback 495.5→0、动作 -416 | `sim/cli.py` 与 `run_drill.py` 的 `Q3BatchPolicy` 改回 `Q3Policy`（2 个文件各 1 行） | ✅ 已切入口默认（全量回归通过）；回退保留在注释 |
| A1 排除圆（use_no_signal_pruning） | exp2_pruning.json（300 局两臂零差异） | 中性（Q3 全向下从不触发） | — | ❌ 不翻（防御性保留） |
| A2 直径目标（continuous_objective） | exp3_diameter.json（40 局） | 时间 -576s 但现实规划成本爆炸+门1 c 条个别局上升 | — | ❌ 不翻（转队列优化成本） |

## 未决问题

- Q3Policy 参数面快速膨胀（scan_layout/use_no_signal_pruning/continuous_objective/
  use_optimal_stop/stop_cost_per_metre/guess_clear_threshold_m/max_refinements/
  error_deg/fim_*）：翻默认只做顶层几个，其余保持 False 并归档为论文配置表。

## 晨间人工清单（草稿）

1. 官方演练：当前默认配置（pure_ring8 + max_actions 8000）跑 1-2 局 13+ 源，
   确认动作上限不再触发（验证 8000 修复）。
2. 对每项翻默认的优化：3-5 局官方对账（cleared==官方 且 c+a==20），异常按
   决策表回滚命令一键回退。
3. Q4 由另一位队员负责（不在本任务范围）。

## 后续会话队列

（待填）