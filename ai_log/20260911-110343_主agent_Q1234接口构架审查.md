# AI 使用记录：Q1234接口构架审查

## 输入

```text
阅读当前项目构架，告诉我Q1234的接口分别在哪里？如何调用？是否被嵌套调用？有哪些现有功能不是必须？
```

## 运行结果

已审查 `problem_b/` 中Q1至Q4的公开入口、CLI、内部调用链、运行器和客户端接口，并用Python导入及 `inspect.signature` 验证各主要接口签名和Q4继承关系。结论：Q1入口为 `plot_cli.analyze_q1`/`geometry.localize`；Q2入口为 `q2.plan_second_point`，高级入口 `plan_measurement` 被Q3细化阶段调用；Q3/Q4以 `Q3Policy`/`Q4Policy` 实现统一策略接口，经 `runtime.runner.run_policy` 和Fake/HTTP客户端执行；Q4继承Q3。Q3/Q4离线CLI把 `max_refinements` 设为0，因此演示不实际调用Q2。识别出旧浏览器实验链、参考真值标记、FIM对照、测试oracle和FakeSimulator等非正式运行必需功能，并区分了可移出提交入口与应保留作验证的内容。
