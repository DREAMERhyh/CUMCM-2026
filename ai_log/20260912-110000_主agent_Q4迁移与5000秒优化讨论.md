# 输入

1.Q3的做法可以直接迁移至Q4吗？预计能有多少提升？这一步不修改文件。2.帮我思考有没有其他的优化方向，期望最终可以将平均虚拟时间下降至5000，这一步只探讨大致思路，不需要细节实现。

# 运行结果

完成只读分析与方案讨论，未修改项目代码、方案文档或状态表。结论是：Q3 的执行框架、清除覆盖和总时间滚动优化思想可迁移到 Q4，但必须把候选状态扩展为“位置—发射方向”并正确处理不可见分支；Q3 的七点全向覆盖、距离保证接收、原地失败复测不能直接沿用。当前 Q3 离线小样本均值为 5873.934 秒，达到 5000 秒还需下降约 14.9%；优先方向为清除网格加稀、多源联合路径规划和扫描阶段降本。Q4 尚无成对基准，任何收益数字只能作为待验证目标；其首要优化是减少 121 点定向覆盖保底的平均扫描开销，同时保留严格终止证书。
