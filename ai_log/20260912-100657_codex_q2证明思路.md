# AI 使用记录：Q2 证明思路

## 输入

```text
本次对话的目标是完成论文的问题2部分，首先阅读paper里的readme和solution里关于问题2的部分，首先给出证明思路

这是数学建模论文，不应该以纯数学的证明为核心，应该主要关注选点策略，计算函数，方法的正确性可以用命题补充，但不是作为行文主线

那么问题1中提出的圆弧问题如何解决？

紧密联系问题1，完整的从问题分析，模型假设，模型建立，模型求解等给出论文思路，我进行确认

1. 论文写作符号体系与前文保持一致，不管solution文档
2. 程序的余量可以写上，明确说明保证边界安全的意图，但不要长篇大论
3 4 按你的想法来即可，可以开始写

fim行列式的部分有必要的情况下加上引用

我下面列出问题，进行修改：
1. 格式上每个equation环境后必须空一行接正文。不然换行缩进不会生效，除非你认为需要更紧密的衔接，这一项包括问题1
2. 问题2里有很多类似“物理”可行域、“物理”候选区域的措辞，这个物理貌似比较冗余，删去。
3. 最小包围圆的计算方法应更详细，不能只说枚举一至三个顶点...
4. 带过解释Minkowski和的含义
5. 作图，分别按readme要求画出5.2.1 K_1和\widehat K_1之间的关系，应画出圆弧与近似外切正多边形
6. 补充说明这里的多边形近似是否承接自问题1中对圆弧边界的讨论，在代码中验证这一点，如果有疑问先提出
7. 尝试画出5.2.3中的F_p, F_g，如果较难则保留占位符，后续人工作图
```

## 运行结果

已按确认口径修改 `paper/paper.tex`：新增问题二分析、问题二模型假设和新增符号表；以问题一的 `K`、`hat K`、直径圆覆盖结论为起点，完整写入圆弧外切处理、最小包围圆指标、保证/可能接收域、有限场景后验评分、离散候选基线、连续 Fisher 信息多预算搜索、统一复评分、Pareto 裁决、最终推荐安全命题、近优候选区域和求解复杂度。理论示向误差写为 1 度，程序 1.005 度仅说明为边界安全裕量。Fisher 信息矩阵首次出现处引用 Fisher 1922 原始文献，并在参考文献中加入 DOI。静态检查确认 37 个标签无重复、无缺失交叉引用或参考文献，所有 LaTeX 环境和花括号配对；本机无 XeLaTeX、latexmk 等 TeX 引擎，未验证最终 PDF 排版。执行 `python -B -m unittest discover -s solution/tests/q2 -t solution -v`，9 项测试全部通过，耗时 47.743 秒。未修改 Q2 代码或人工验证状态。

本轮按论文现有问题一正文重新梳理 Q1-Q2 衔接：问题一的纯示向多边形 `Omega` 在加入目标圆和接收上界后成为含圆弧的物理域 `K`；问题二针对一次观测构造 `K_1` 并以外切正多边形得到不漏真源的 `hat K_1`。同时利用问题一直径圆未必覆盖区域的结论，问题二改用最小包围圆半径评价后验定位不确定性。给出从问题分析、模型假设、符号复用、圆弧处理、候选区域、有限场景评分、离散基线、连续 FIM、多预算复评分、Pareto 裁决到近优域输出的完整论文结构，并列明有限场景、数值搜索及内近似判空的表述边界。本轮未修改论文正文或算法代码，未运行测试，等待队员确认结构。

补充：问题一遗留的圆弧在问题二中按用途作多边形近似。源位置区域中的目标圆和接收圆采用 24 边外切正多边形，保证不漏真源；保证接收域中的 1000 m 圆盘采用 72 边内接正多边形，保证返回域内的点真实可行；可能接收域采用有限支持方向外近似。计算得到两类 24 边外切圆的最大径向外扩约为 15.532 m 和 12.943 m，72 边内接 1000 m 圆的最大径向收缩约为 0.952 m。未修改论文正文或 Q2 代码，未运行算法测试。

已阅读 `paper/README.md`、`paper/paper.tex` 的问题二占位与问题一衔接段、`solution/src/q2/README.md`、`solution/Q2数学建模与双方案比较.md`、Q2 教学材料，以及候选域、FIM 和规划器的真实实现。根据队员纠正，将论文叙事调整为“源区域和可行域—离散鲁棒选点—连续 FIM 选点—统一复评分与混合裁决—候选区间和结果分析”，重点说明选点策略、计算函数、约束和求解流程。保证接收的顶点判据、FIM 行列式来源和最终推荐相对基线的不劣性仅作为三个辅助命题。有限场景、模式搜索和近优域凸包明确作为数值近似，不宣称连续全局最优；计算保证域为空也只说明内近似未提供证书。本轮未修改论文正文或 Q2 代码，未运行测试。

按本轮七项意见修改 `paper/paper.tex`：统一使问题一、问题二的每个 `equation` 环境后留出空行；删除问题二“物理可行域”“物理候选区域”等冗余措辞，仅保留问题一对题设物理条件的说明及“量纲和物理含义”等必要用法；将最小包围圆展开为单点、两点直径圆、三点外接圆、共线跳过、逐顶点覆盖检查和最小半径选择，并给出最坏复杂度；简要解释 Minkowski 和。新增问题一圆弧讨论到问题二外切正多边形构造的明确承接段。新增 `paper/code/q2_region_figures.py`，复用现有 Q1 角区半平面、半平面求交和 Q2 候选域函数，生成 `paper/figures/q2-k1-outer-approx.{pdf,png}` 与 `paper/figures/q2-reception-regions.{pdf,png}` 并插入正文。首次生成接收域图时 Matplotlib 因 `\\mathcal F` 写法报错，修正为 `\\mathcal{F}`；首次视觉检查还发现局部图外的 `S_1` 标注拉大画布，设置裁切后重新导出，最终 PDF 渲染检查无文字溢出、裁切或不可读图例。脚本断言示例中 174 个高密度 $K_1$ 边界点均位于 $\widehat K_1$ 内；运行时还确认 Q2 域构造所引用的 `bearing_planes` 和 `intersect_halfplanes` 与 Q1 实现是同一函数对象，且返回元数据为 24 边外切、保守近似。静态检查确认 57 个 equation 后均有空行，42 个标签无重复，LaTeX 环境配对，无缺失交叉引用或缺失文献。重跑 Q2 单元测试 9 项全部通过，耗时 48.608 秒。当前环境存在 `pdftoppm` 与 `pdfinfo`，但无 XeLaTeX/latexmk，整篇论文最终分页、字体和引用编号仍待 Windows 双次编译人工检查。未修改 Q2 正式代码或人工验证状态。
