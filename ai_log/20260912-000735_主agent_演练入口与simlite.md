# AI 使用记录：演练入口补齐与 simlite 批量回放

## 输入

```text
继续本项目工作。今天是9月12日，正式测试必须在9月13日15:30前完成，时间紧迫，
本轮工作的第一阶段产出必须是"让我（人类队员）能立刻上手操作的手册"。
先读取 AGENTS.md、ai_log/ 全部历史记录、runtime/、q3/cli.py、q3/policy.py、
q3/coverage.py、common/、README，然后严格按以下顺序执行：

[第0步] 环境与入口盘点（只读）：真实模拟器入口、scan_layout 支持、日志落盘路径、
Q3Policy 默认 scan_layout。
[第1步] 补齐真实模拟器入口的 scan_layout 支持（小改动）+ 测试 + 全量回归。
[第2步] 输出《演练对账操作手册》docs/操作手册_演练对账.md（六节 A-F，含 CSV 模板、
判定回退规则、时间节点）。
[第3步] 暂停，等我执行手册。
[第4步] 等待期间若可连续工作则执行：任务4a 默认布局切换 ring7→pure_ring8；
任务4b 新建 simlite/（函数级直调模拟器，附件2 语义严格一致，199 秒示例验收 +
Q3Policy 端到端对账 + N 局并行回放）；任务4c 搜索深度增益实验
（fim_max_iterations 120/600/3000，增益<1% 关闭此线）。
[第5步] 等我反馈对账 CSV 后收尾。
（硬性禁止：不动 HTTP 协议层/幂等/保底/near；无概率假设；无 Git；时间复用
common/time_model；新代码有测试；完成后写 ai_log。）
```

## 运行结果

### 第0步 盘点结论

- 对接真实模拟器入口是 `src/sim/cli.py`（runtime/runner.py 只是通用离线策略驱动；
  runtime/http_client.py、fake_simulator.py 是兼容导出）。robot_id 用 `--robot-id`
  传入（必须显式；新增 run_drill 支持环境变量 ROBOT_ID 回退）。
- 真实模拟器入口此前**不支持 scan_layout** → 第1步已补。
- 日志落盘：逐动作 JSONL 默认 `output/sim/q{problem}_{时间戳}.jsonl`；
  但 cleared/absent 此前只在终端打印（未落盘）→ run_drill.py 补齐摘要 JSON。
- Q3Policy 默认 scan_layout 当时为 ring7（第4步已切换为 pure_ring8）。

### 第1步 真实入口 scan_layout + 一键脚本

- `src/sim/cli.py` 新增 `--scan-layout {ring7,hub_ring6,pure_ring8}`（仅 --problem 3
  生效；缺省 None 跟随 Q3Policy 当前默认）。
- 新增 `solution/run_drill.py`：一键启动 = sim/cli 同款安全门（--confirm-ready /
  --confirm-policy 必须显式，否则不发请求）+ robot_id 环境变量回退 +
  逐动作 JSONL + **时间对账**（用 common/time_model 重算移动/切换/检测/清除拆分
  对照官方 virtual_time_s，不一致即打印警告）+ 摘要 JSON 落盘
  （cleared/absent/虚拟时间/平均定位清除时间）。
- 说明：用户提醒"sim 文件夹就是对接模拟器的接口代码"——已核实 sim/cli.py 确为
  官方入口，run_drill.py 是包装层（结果落盘 + 对账是 sim/cli 没有的硬需求），
  未删除、未重复造协议层；已在对话中向用户说明取舍理由。
- 新增测试：`tests/sim/test_run_drill.py`（12 项：官方 199 秒示例对账、
  mismatch 检出、摘要落盘、门禁、scan_layout 透传）、
  `tests/sim/test_cli_scan_layout.py`（4 项）——全部通过。

### 第2步 手册

- `docs/操作手册_演练对账.md`：六节（A API 确认流程逐字段表；B 模拟器下载/注册/
  登录/端口；C 演练启动与 run_drill 精确命令；D 对账 CSV 模板与来源列；E 判定与
  回退规则；F 时间节点 15:30/17:30）。详见交付回复。

### 第4步

- 4a：Q3Policy 默认 scan_layout ring7→**pure_ring8**（覆盖解析 984.21m≤995、
  扫描虚拟时间省 460s）；tests/q3/test_offline.py 中依赖旧默认几何的用例显式传
  ring7（不削弱断言）；新增默认值断言；PROJECT_CONTEXT/sim README 同步。
- 4b：新建 `src/simlite/`（只新增文件，零修改 common/q1-q3）：
  - `Simulator`：微秒整数累计虚拟时间；enter/measure/clear/exit 完全按附件2
    （移动 5m/s、切换 1s、检测 5s、clear 3/5s、20m 半径、三态、定向 ±90° 含边界、
    每（地点,频道）固定误差 ±1°、request_id 幂等/冲突拒绝）。
  - `generate_sources`：10-16 源、频道不放回、位置面积均匀 D(0,1800)、
    R~U(1000,1500)、定向比例参数、seed 可复现。
  - `replay.run_episode / replay_batch / summarize_episodes`：端到端一局/多局
    （多进程）统计：cleared/absent、扫描/resolve/清除动作三阶段虚拟时间、
    平均定位清除时间、逐动作 time_model 对账（accounting_ok）。
  - 验收硬标准1：官方 199 秒示例逐时刻精确复现（0/105/111/194/199/199 ✓）。
  - 验收硬标准2：默认 Q3Policy 端到端一局对账通过（差值 <1e-3s）✓。
  - tests/simlite/test_simlite.py：20 项全绿（约 90s，其中 4 局完整 Q3 策略）。
- 4c：`tests/q2/benchmark_depth.py` 搜索深度增益实验（120/600/3000 迭代档 × 5 个
  代表性场景，种子/最小步长同比例放宽，保险预算 360s），结果写入
  `tests/q2/analysis/depth_gain_experiment.json`。
  实测：15 次规划全部自然收敛无截断；worst_radius 从 120 档到 3000 档
  变化为 +0.02% ~ -0.05%，**中位增益约 +0.02%（远小于 1%）**；
  个别场景深档反而略差（确定性搜索轨迹差异）；墙钟几乎不增（5.7→11.2s）。
  **结论：关闭此优化线，保持默认 120 迭代档**；要借预算提升效果需改
  搜索结构本身（如模式方向数），不在本轮范围。

### 检查与验证

- 新增测试：sim（16 项）、simlite（20 项）、q3 布局（25 项含既有）全部通过。
- 全量回归（最终）：**130/130 通过（213s）**。中途发现既有时序敏感点：
  q2.test_unit 的近优域 5s 子预算在重负载套件下抖动 → 该测试显式给足
  近优域预算（near_optimal_region_cpu_limit_s=15，断言语义不变），
  产品默认配置未动（线上 Q3 本就关闭近优域，不受影响）。
- 未修改：HTTP 协议层、request_id 幂等、certificate/fallback 保底、near 处理、
  FIM 算法本体；无 Git 操作。

### 第5步：8 局演练对账分析（2026-09-12 01:10 用户反馈后）

用户完成 8 局（--problem 3、pure_ring8，其余默认；官方案例编码未抄录，
以其输出文件时间戳为 test_case_code，note 注明"非官方编码"）。逐局判定：

| 局 | 时间戳 | 官方 | 清 | 缺 | c+a | 一致 | 安全退出 | 对账差 | 总虚拟s | 扫描段s |
|---|---|---|---|---|---|---|---|---|---|---|
| 1 | 004941-787471 | 15 | 12 | 5 | 17 | FALSE | action_cap | +1μs | 13977.7 | 2180.8 |
| 2 | 005139-833629 | 14 | 13 | 6 | 19 | FALSE | action_cap | -3μs | 15727.0 | 2066.2 |
| 3 | 005537-486927 | 12 | 11 | 8 | 19 | FALSE | action_cap | +3μs | 13088.3 | 2063.7 |
| 4 | 005924-381010 | 15 | 15 | 5 | 20 | TRUE | - | +2μs | 11753.6 | 1885.0 |
| 5 | 010109-225646 | 11 | 11 | 9 | 20 | TRUE | - | -2μs | 11501.1 | 2180.3 |
| 6 | 010245-794247 | 13 | 13 | 7 | 20 | TRUE | - | +1μs | 11108.3 | 2154.2 |
| 7 | 010427-522083 | 10 | 10 | 10 | 20 | TRUE | - | 0μs | 8704.7 | 2199.7 |
| 8 | 010638-445314 | 10 | 10 | 10 | 20 | TRUE | - | +2μs | 7991.4 | 2016.9 |

- **口径修正**：用户手册 E 节原"cleared+absent == 官方总数"公式在数学上不成立
  （absent 含判为无源的频道，两者之和与"有源频道数"没有相等关系；
  正确一致 = cleared == official_total 且 c+a == 20）。已修正手册 D/E 节与
  drill_analyze.py，并向用户说明。
- **关键结论①**：局1-3 是 `action_cap_safety_exit`（客户端动作上限 1000 被触发，
  14-16 源局的 resolve+fallback 动作数超 1000；现实耗时仅 50-61s，不是超时），
  **非策略漏检**。修复：sim/cli.py 与 run_drill.py 的 Q3 policy 默认
  max_actions 1000 → 8000（日志 ~1.6MB < 2MB 上限）。fallback 保底逻辑未动。
- **关键结论②**：完成局 4-8 共 5 局全部 TRUE → **布局锁定 pure_ring8**（手册 E 节
  规则满足；局1-3 未完成不计入一致但也不否定覆盖结论）。
- **扫描段**：首个 /clear 前的累计虚拟时刻（含首源定位细化，非纯扫描），
  与离线基准 2172.7s 偏差 -13.2%~+1.2%；下行偏差主因是"已发现频道跳过后续
  测量"（源越多、发现越早、跳过越多），上行偏差来自首源 refine 测量，
  均非布局问题（simlite 无源局 = 2172.65s 已证明）。
- **测试**：tests/sim/drill_analyze.py 新增（可复用的对账分析脚本，支持
  Q4 局与追加 CSV）；sim+q3 相关 60 项回归通过。
- **用户裁决已执行**：run_drill.py 保持现状不合并；搜索深度优化线关闭
  （维持 120 迭代默认）；ring7 对比局降级为可选（全部一致且时间富余才跑）。
- 待办推送：Q4 演练 2-3 局为今天第一优先（Q4 策略从未真机验证）；正式测试
  准备项、表1 四字段取值（测试案例编码/清除干扰源个数=summary.cleared_count/
  平均定位清除时间=summary.average_clear_time_s/程序运行时间=enter 到 exit
  现实时长，取自 run_drill 摘要 + 模拟器界面）；正式测试案例编码必须抄 GUI
  官方编码（非文件时间戳）。