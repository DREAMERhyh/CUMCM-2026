# AI 使用记录：4-opt 时间统计

## 输入

```text
对output/sim/4-opt/中的日志做同样的汇报
```

## 运行结果

读取了 `solution/output/sim/4-opt/` 的 5 个 Q4 JSONL。沿用前两次任务口径，将一个成功清除的信号源视为一个“点”：单点虚拟时间为最终累计 `virtual_time_s` 除以成功清除数，单点真实时间为退出记录 `elapsed_real_s` 除以成功清除数。

5 次运行等权平均为 1139.899105 虚拟秒/点、1.243420 真实秒/点，共成功清除 62 点。按全部成功点合并加权为 1134.967567 虚拟秒/点、1.249984 真实秒/点。单点虚拟时间样本标准差 82.985196 秒，真实时间样本标准差 0.099737 秒；失败清除率为 132 次清除尝试中的 70 次，即 53.030%；测量动作数/成功点为 31.854839。

5 个日志均检查为 HTTP 200、`accepted=true`、最终动作为 `exit`，累计虚拟时间单调不减。文件属于 Q4，前两次 legacy/new/3-opt2 文件属于 Q3，未将二者直接作算法性能比较。
