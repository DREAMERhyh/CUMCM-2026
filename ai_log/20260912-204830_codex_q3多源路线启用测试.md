# 输入

可以打开多源路线优化测试一下能否跑通

# 运行结果

未修改代码默认值，使用固定种子 20263912 的 1 个多源合成场景（15 个不同频道），按 FIM/滚动/路线真实墙钟软截止 10/1/0.25 s，同时运行路线关闭 `current` 与显式启用 `insertion_2opt` 的完整 Q2→Q3→FakeSimulator 流程。命令正常退出，输出到 `solution/output/q3_offline/route_smoke_after_merge_20260912.{json,md}`。

两组都完整清除预期的 15 个频道且无错误。路线关闭组为 118 动作、6237.636 s 虚拟时间；启用组为 117 动作、5666.931 s，减少 570.705 s。启用组实际调用路线规划 30 次，状态为 `ok=21, fallback=9`，证明多源路线模块确实参与决策且超时时安全回退；定位阶段移动减少 2838.525 m，跨源移动减少 2296.423 m，但长跳次数增加 1、长跳距离增加 148.943 m，真实 CPU 增加约 5.897 s。单场结果只用于跑通和初步迹象，不代表稳定性能或官方模拟器成绩。运行时仍出现环境层 stream fd 警告，但退出码为 0、结果文件完整。
