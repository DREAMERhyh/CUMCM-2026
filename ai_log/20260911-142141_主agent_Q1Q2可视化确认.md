# 输入

当前Q1和Q2可以可视化吗

# 运行结果

确认当前 Q1 和 Q2 均有可运行的可视化入口。Q1 使用 `solution/src/q1/cli.py` 和 `bearing_plot.py`，支持窗口显示及 PNG/SVG 输出；Q2 使用 `solution/src/q2/cli.py` 和 `plot.py`，支持窗口显示及图片、JSON 输出。已运行两个 CLI 的 `--help`，参数加载正常；本次未重新生成图形。
