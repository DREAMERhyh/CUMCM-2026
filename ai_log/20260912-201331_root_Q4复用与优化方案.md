# AI 使用记录：Q4复用与优化方案

## 输入

```text
Q3已经基本完成优化，当前Q4有哪些可以复用的内容？以及Q4有哪些需要单独优化？先商讨方案。
```

## 运行结果

完成只读方案审计，未修改业务代码。结论是 Q3 的运行状态机、正向示向后的保守位置区域、近距离清除、后验清除网格、时间模型、通信层，以及路线/缓存的通用求解骨架可以复用；Q3 基于“距离足够即保证接收”的单点 FIM 裁决、联合批测、失败清除复测和位置场景滚动评价不能直接用于定向源，须增加位置—发射方向联合状态与 `no_signal` 分支后再接入。发现当前 Q4 默认最多两次细化，但四侧可见性证明需要完整四点集合；四侧构造失败时又回退到只含距离条件的 Q2 点，这是正式优化前的首要正确性缺口。建议按“正确性基线—扫描降本—联合后验与滚动优化—多源路线”四阶段推进，并以同场景配对、独立种子和极端方向案例验收。现场重跑 `python -B -m unittest tests.q4.test_offline -v`，4/4 通过，耗时 0.053 s；这些测试未覆盖上述默认两次细化与不安全回退，也未连接官方模拟器。
