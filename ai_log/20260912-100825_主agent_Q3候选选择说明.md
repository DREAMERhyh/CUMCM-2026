# 输入

当前Q3使用的是混合顶层推荐还是单纯FIM？我应该选择哪个？

# 运行结果

完成只读核对。当前默认 Q3 不是单纯 FIM，也不只是机械执行 Q2 顶层推荐，而是把 Q2 顶层混合推荐、离散基线、连续 FIM 多预算候选、Pareto 候选和保证接收域代表点汇入候选池，再以有限场景总虚拟时间滚动评价选择测点或直接清除。`prefer_continuous_fim=True` 只在关闭滚动总时间模式时优先选择连续 FIM 点。建议保留当前滚动混合策略；若只能在 Q2 顶层混合推荐和单纯 FIM 之间二选一，应选择顶层混合推荐。未修改算法或文档，未执行 Git 操作。
