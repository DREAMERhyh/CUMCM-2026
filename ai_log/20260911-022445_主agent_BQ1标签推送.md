# 输入

给当前的版本打一个tag为B-Q1，并且推送到远端

# 运行结果

核对当前分支为 `main`，目标提交为 `b859d412e035271714686cb5abe1255ce4bc34b7`（`feat: finish B-Q1`），且推送前 `origin/main` 已指向该提交。已在该提交上创建附注标签 `B-Q1`，并推送到远端 `origin`。通过本地 `git show-ref --tags --dereference B-Q1` 和远端 `git ls-remote --tags origin` 交叉验证，标签均解引用到目标提交。未提交或修改现有未跟踪的 `simulator/` 目录及其他日志文件。
