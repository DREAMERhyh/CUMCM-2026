## 输入

现在大量使用“得到证书的频道”表述。不要使用“证书”，改为“完全定位”之类的含义。告诉我你打算怎么改。

## 运行结果

检查了论文与问题三代码中“证书”相关用语。本轮按用户要求仅给出修改计划，未修改论文正文。计划在论文中区分三类含义：将半径不超过 19.9 m 的状态表述为“完成可靠定位”或“达到可靠清除精度”，将相反状态表述为“尚未完成可靠定位”或“定位精度不足”，将“终止证书”改为“退出条件”或“完成条件”；同时拟把论文符号 $r_{\mathrm{cert}}$ 改为 $r_{\mathrm{loc}}$，并修改对应公式标签。为避免无关接口变更，暂不修改 Python 内部的 `certificate_ok`、`certificate_failed` 和 `certified_clear` 等标识符。
