# AI 使用记录：Q2 后段措辞与策略区域重写

## 输入

```text
下面修改5.2.6-5.2.8的一些措辞
1. 5.2.6的“几何病态”
2. 5.2.7的“混合裁决”
3. 5.2.8完全重写，题目不是要求给出一个候选点，而是给出策略和区域。直接把区域定为一定可以接收到信号的区域，把策略重述一遍，并说明题目二只是得到一个点之后的策略，我们在此基础上推广到已有多个点并在q3,q4中运用
4. 5.2.6的“全局最优证书”，不要用证书，可以视语义改为最优选择/最优解/最优证明等。
```

## 运行结果

修改了 `paper/paper.tex` 的第 5.2.6--5.2.8 节及问题二概述。第 5.2.6 节将“定位几何病态”改为“两次方向约束近似重复、交会定位结果对角度误差较为敏感”，将“全局最优证书”改为“不能证明所得结果是非凸问题的全局最优解”。第 5.2.7 节标题由“同口径复评分与混合裁决”改为“统一评价与最终选点”，并明确 Fisher 搜索只寻找可能具有较好交会几何的连续测点坐标，最终仍由统一的定位不确定性半径和行动时间评价决定。

完全重写了第 5.2.8 节，删除原 5%/10% 近优域及其公式。新正文明确：题目二要求的是随首次检测结果变化的选点策略和候选区域，不是固定候选点；将理论保证接收域作为第二检测点候选区域，实际输出其可计算的多边形内近似 `F_g-hat`，可能接收域只作辅助分析。重新归纳了单次已有观测下的完整选点流程，并说明推荐点是策略代入具体输入后的输出。

补充了多观测推广：已有 `n` 次有效检测时，用全部约束更新当前区域 `K_n-hat`，重新构造保证接收域并选择第 `n+1` 个检测点，形成递推策略。结合代码边界说明 Q3 直接将全部已有观测形成的当前区域交给该选点流程；Q4 沿用多观测区域更新与定位不确定性评价，但因定向辐射不能沿用全向源的保证接收结论，下一测点改用多方向接近的探测点。

执行静态 LaTeX 检查：花括号、81 个环境、42 个标签与引用、8 个插图路径均正常；“几何病态”“混合裁决”“证书”、5%/10% 近优域及已删除公式标签均无残留。此次未修改代码和图件，未运行算法测试；当前环境没有 XeLaTeX，尚未检查整篇 PDF 的分页和公式编号。
