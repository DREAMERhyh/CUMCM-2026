# AI 使用记录：runtime用途核对

## 输入

```text
现在runtime还有用吗
```

## 运行结果

已核对当前引用关系。`runtime/runner.py` 仍被 Q3、Q4 离线 CLI、离线测试和 sim smoke 测试实际使用；`runtime/protocol.py` 定义通用 `RobotClient`，但当前没有代码导入；`runtime/http_client.py` 与 `runtime/fake_simulator.py` 仅为旧导入路径的兼容转发，仓库内部已无实际引用。建议保留精简后的通用 runtime（`runner.py`、`protocol.py`、`__init__.py`），确认没有仓库外脚本依赖后可删除两个兼容转发文件。
