# AI 使用记录：Q4 长尾与多源路径实施

## 输入

```text
分为两部分实施。第一是针对本次长尾的优化，按你的方案做。第二是加入Q3的多源路径规划 。你可以对两个部分分别测试单独1，单独2，和合并三组offline测试，给出优化效果，并判断是否予以保留
```

```text
暂停，保持上下文，后面继续
```

```text
继续
```

## 运行结果

已完成两部分实现、独立开关、文档、回归与离线配对。长尾优化收敛为同源连续 8 次清除失败后最多一次、由有限联合后验均值收益门控的一步移动方向探测；若拒绝、失效、超时或实际 `no_signal`，继续原完整安全清除尾。原日志局部状态回放确认第 8 次失败后旧策略继续 clear、新策略改为 measure。多源路线只复用 Q3 第一部分服务块、最便宜插入和确定性 2-opt，以 `route_active_channel` 锁定选中源直至原 Q4 单源策略完成；求解回退时也锁定回退源，未接入联合批测或 beam/cache。Q4 README、现场命令、人工清单、快速上手、TODO 和长期上下文已同步。

固定种子 20269912、8 场景、单进程四模式均 8/8 完整清除且无源证书正确。baseline、仅长尾、仅路线、合并的平均虚拟时间分别为 15450.744、15450.744、15171.465、15163.679 s；路线与合并均 6/8 胜。路线平均移动由 65469.345 降至 63943.577 m，但失败清除由 20.500 增至 24.125，墙钟由 10.533 增至 13.819 s。仅长尾在随机集检查 2 次但启动 0 次，与基线动作完全相同。结论：长尾救援以默认 `adaptive` 保留；路线以可选 `insertion_2opt` 保留但默认 `off`。结果为 `solution/output/q4_offline/long_tail_route_benchmark_sequential.{json,md}`。Q4/CLI 定向回归 19/19 通过；全项目 137/137 通过，耗时 90.111 s。未连接官方模拟器，Q1-Q4 状态未改，仍为【待验证】。
