# AI 使用记录：Q3 路径规划第二部分

## 输入

```text
继续第二部分，现在的优化效果是平均450s，昨天是550左右
```

## 运行结果

已按用户授权完成第二部分。新增 `solution/src/q3/cache.py`，实现策略实例内的有界 LRU 缓存及稳定状态键；缓存接入 Q2 计划、直接清除、固定点批测、27 m 清除网格/路线、候选场景、服务块和束搜索下界。`q3/route.py` 新增有限束搜索，以第一部分插入+2-opt解为完整保底；`q3/policy.py`、Q3 本地 CLI 和在线 CLI 增加 `beam_cached`、缓存容量、束宽和扩展上限配置，Q4 与正式默认仍保持关闭。新增束宽训练脚本，三策略配对脚本扩展为 current/insertion_2opt/beam_cached，并同步更新 Q3 README、TODO、路径规划指导文档和 PROJECT_CONTEXT。

训练种子 20264912 的 1 个场景中，束宽 1/2/4 虚拟时间相同，束宽 1 现实墙钟最低，故锁定束宽 1、缓存容量 4096、最大扩展 512、束搜索路线软截止 1 s。验证使用第一部分相同种子 20263912 的 4 个场景；三策略均 4/4 完成。beam_cached 相对 insertion_2opt 为 0/4 胜，平均总虚拟时间从 5236.592 s 增至 5417.769 s，P90 从 5876.333 s 增至 5918.775 s，平均跨源移动增加 1306.861 m；缓存命中率 77.9%，平均现实墙钟从 206.465 s 降至 194.052 s。按预设判据保留 insertion_2opt 为正式候选，beam_cached 仅作实验模式。结果保存于 `solution/output/q3_offline/route_part2_beam_train.json` 和 `route_part2_benchmark.{json,md}`。

Q3 定向测试 37/37 通过；最终文件状态下全项目单元测试 112/112 通过，耗时 68.052 s。所有性能测试使用本地 FakeSimulator，未连接官方模拟器。实施中曾发现缓存关闭时仍构造缓存键会给软截止增加额外开销，已改为缓存关闭时完全绕过缓存路径并重新配对。用户提供的当前平均约 450 s、昨日约 550 s 改善作为运行波动背景记录，没有与本轮新增算法收益混算。
