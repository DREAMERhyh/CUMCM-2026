# AI 使用记录：solution 源码清理审查

## 输入

```text
读取当前最新的solution/src中的内容。告诉我哪些函数或代码文件是被弃用、或者是实验性内容但是没有被当前启用的，我准备清理代码目录，当前不修改文件。我们的测试指令是：python run\_drill.py --problem 4 --robot-id 202601101010 --confirm-ready --confirm-policy和python run\_drill.py --problem 3 --mode policy --robot-id "202601101010" --confirm-ready --confirm-policy --max-actions 8000
```

## 运行结果

完成只读审查 `solution/src`（53 个 Python 文件及 legacy 前端资源）。确认两条指令分别实例化 `Q3BatchPolicy` 与 `Q4Policy`，并核对其运行时默认参数、静态导入/调用关系、`tests/` 下单元测试、离线回放和实验脚本引用。

高置信整文件清理候选为：已明确废弃且无现有测试引用的 `legacy/server.py` 与 `legacy/visualization/{app.js,index.html,style.css}`；无仓库 Python 调用者的兼容转发 `runtime/http_client.py`、`runtime/fake_simulator.py`；无人引用的 `runtime/protocol.py`。`legacy/environment.py` 因 `tests/legacy/test_environment.py` 依赖而应保留。

另识别出若干函数级死代码：`q2/continuous_diameter.py` 中被后一定义覆盖的首个 `_diameter_at`，`q3/refine_selector.py:_candidate_directions`，`q4/directional.py:four_sided_points`，`q4/belief.py` 中未调用的 `source_kind`、`visible_weight`、`outcome_weights`，`common/budget.py:used_s`，以及 `simlite/paired.py` 的 `_difference`、`_pair_series`。还发现少量无效导入。

Q3 多个默认关闭实验分支（tour/minimax/enroute/two-leg/batch-guess/tour-aware-marginal、无信号修剪、鸽笼早停、diameter 目标）和 Q4 备用扫描/保底/关闭模式均不走当前现场指令，但因离线测试或实验脚本引用应保留。特别核实：`tour_guess`、`tspn_clear`、`skip_low_value_refine` 当前实际为开启，与附近“默认关闭”注释存在不一致；`enroute_refine` 的专用响应块受前置 mode 判断阻断而不可达。未执行用户给出的官方模拟器指令，未修改业务源码。
