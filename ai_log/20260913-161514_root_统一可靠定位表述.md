## 输入

可以，就这么改

## 运行结果

按上一轮确认的方案统一修改论文术语：将“取得证书”类表述改为“完成可靠定位”“达到可靠清除精度”或“尚未完成可靠定位”；将 $r_{\mathrm{cert}}$ 改为 $r_{\mathrm{loc}}$；将“终止证书”改为“任务完成条件”，并把公式标签 `eq:q3-exit-certificate` 改为 `eq:q3-exit-condition`。修改覆盖问题分析、模型假设、符号说明和问题三正文；未修改 Python 内部标识符及程序逻辑。检查确认论文 `.tex` 文件中不再出现“证书”、`r_{\mathrm{cert}}` 或旧公式标签。
