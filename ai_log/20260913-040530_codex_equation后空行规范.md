# AI 使用记录：equation后空行规范

## 输入

```text
在readme中新增：equation环境后必须空一行，以保证后面正文的缩进生效
```

## 运行结果

已在 `paper/README.md` 的“论文建模写作规范”中新增规则：每个 `\end{equation}` 之后必须保留一个空行，使后续正文成为新段落并按模板规则缩进。本次仅修改 README，未修改论文正文。
