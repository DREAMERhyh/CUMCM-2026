# 实验轮 6：整晚自主探索 · 进度与断点文件

> 2026-09-13 凌晨起。目标：官方 405 → 330-360 s/源（混合地板 350-370）。
> 官方基线：11 局均时 ~405（M9+tspn 已官方生效）；墙钟仅 ~2 分钟/25 分钟，
> 现实计算解禁；simlite 一夜可评估 10⁴-10⁵ 局。

## 规则核查包（完成，先落结论）
- a) 已发现频道可否豁免覆盖矩阵：**成立**。附件2 1.3"每个频道最多对应一个
  干扰源"（保证）→ 源被发现后该频道不可能再有其他源 → 该频道后续驻留点的
  测量对"发现/absent"无任何新增保证 → 纯冗余。**与交织的冲突**：交织驻留点间
  免费腿 = 已发现频道的顺路补测（信息价值高）；跳测须按"交会价值"判据
  （张角 ≥30° 保留、否则省 6s）——实现为猜1'（角度判据交织跳测）。
- b) 每频道至多一源：**保证**（附件2 1.3 原文）。
- c) R 对狗隐藏：**是**（附件2"模拟器接口不会返回有效接收半径"，仅在题设给
  [1000,1500] 界）。→ R 在线辨识（猜2）合法：观察 direction→no_signal 边界。
- d) 单次驻留可测多频道：**是**（B 题附录2(4)：变换频道 1s 后可连续测；当前
  实现已如此）。"检测 800s→40s"不成立：800s = 8 停 ×20 频道 ×5s 的结构代价
  （absent 需全盘覆盖每个频道），除非驻留点数减少（须覆盖数学成立）。
- e) 路径中途可否测量：**是**（"机器狗在任何一点进行检测时先停止移动"——
  任意点可停测）→ 连续覆盖巡回语义成立（轮3 已放弃实施，收益 ~120s 且证明
  负担高，维持放弃）。
- f) 切换成本：任意两频道切换 1s（附件2 4.3）✓ 当前已按此计价。
- 结论：**猜 1'（角度判据跳测）与猜 2（R 辨识）的规则前提全部成立**。

## 猜想实现顺序（自主裁决）
1. 猜1' 角度判据交织跳测（默认关 → 快筛）——预期 +100-150s/局
2. 猜4 探针条带（保底语义不变、实现重造）——预期灾难 1500s→200s（尾部）
3. 猜3 ℓ-判据腿部放置（D2 的最小移动修正版）——预期 FIM 腿 1127→~600m
4. 猜5 巡游 guess、猜6 联合排序、猜2 R 辨识、猜7 CMA-ES —— 按剩余时间
5. 组合大样本 ≥500/臂 → 红队 → 收尾报告 docs/实验轮6_报告.md

## 里程碑
- **猜1'（角度判据跳测）已晋级默认**：30 局门1 全过、-85.6s 中位（扫描段 -104s
  30/30 全降）；回滚 `skip_low_value_refine=False`。
- **猜4（探针条带）已晋级默认**：tail 6768→5862（-906s 最坏局）、120 局确认
  16 改善/104 平/0 恶化；`probe_plan` 蜂窝密排数学覆盖 MEC 盘；回滚
  `probe_strip=False`。
- **猜6（tour-aware 排序）失败归档**：7/12 差——滚动重排序噪声。
- **猜5（巡游 guess）已晋级默认**：快筛 30/30 全优（-460s）、60 局确认
  57/60（-463.7s、-9.4%）——r∈(19.9,40] 难源提前入清除队列由 TSP 巡游
  clear；与 M7 双程失败的对照实证巡游结构价值；c 条净小升（30 局 +89 次
  有界探针尝试）按"收益/风险不对称 + 链有界 + 100% 清除 + 文档化修正"
  晋级；回滚 `tour_guess=False`。
- **收尾中**：最终组合 100 局（任务 bi31ja43t）→ 全量回归 → 红队 → 报告。