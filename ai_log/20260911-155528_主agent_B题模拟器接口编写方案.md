# AI 使用记录：B题模拟器接口编写方案

## 输入

```text
读取B题的附件内容和src/中的代码，在solution/src/中新建一个文件夹，在其中编写和模拟器测试所需的接口文件。如果需要修改已有代码来统一接口，你也可以修改。现在先给出编写方案，不要直接编写
```

## 运行结果

已读取 B 题附件1、附件2的正文、表格及 Word 公式，并检查 `solution/src/` 中 Q1—Q4、common、runtime、legacy 的现有接口及相关离线测试。结论是新建 `solution/src/sim/` 作为官方模拟器协议与客户端的唯一实现位置，保留 `runtime/` 作为通用策略执行层；通过兼容导出逐步替代现有 `runtime/http_client.py` 和 `runtime/fake_simulator.py`。方案包含严格请求/响应校验、同请求幂等重试、串行执行、现实时间截止、逐动作 JSONL 日志、演练测试安全入口及 `tests/sim/` 的本地协议测试。当前 Q1/Q2 算法接口无需改动，Q3/Q4 策略通过既有 `Action` 与 `execute()` 接口接入。

未修改任何方案代码。尝试按文档技能将两个 DOCX 渲染为页面图像，但依赖运行时未提供 `soffice.exe`，渲染失败；随后使用 `python-docx` 和 OOXML 直接核对正文、表格与公式，且已清理失败渲染产生的空临时目录。该限制不影响协议字段和规则的提取，但未完成页面级视觉核验。
