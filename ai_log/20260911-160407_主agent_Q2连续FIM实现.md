# 输入

在src/q2中实现新的FIM连续优化方案，并保持原有的baseline。在新的输出中，我希望同时看到两组结果，一组是之前的离散搜索结果得出的选点分数，一组是新方法得出的选点分数。你可以在同一张图中画两个最终结果。

# 运行结果

在 `solution/src/q2/continuous_fim.py` 新增保证接收域内的连续测点坐标优化：以有限源域边界场景中最小的两示向 FIM 行列式/动作时间为替代目标，使用确定性多起点投影模式搜索。保留旧离散集合评分为 `selected_point`/`selected` 兼容基线，新增 `baseline`、`continuous_fim`、`comparison` 输出，并用旧评分器复算连续 FIM 点，使两组最终分数可直接比较。Q3 显式关闭该额外优化，维持原调用行为。

更新了 Q2 图形和命令行输出：同图以红星、蓝菱形显示两个最终点，并用柱状图展示同口径分数；生成 `solution/output/q2_comparison.png` 和 `solution/output/q2_comparison.json`。同步更新 `src/q2/API.md`、`README.md`、人工验收清单及 `PROJECT_CONTEXT.md`。

检查结果：`python -B tests/q2/test_unit.py` 为 9/9 通过；random、boundary、adversarial 三类既有数据对拍各 4/4 通过；`python -B -m unittest discover -s tests -t . -v` 为 71/71 通过。演示结果为离散基线分数 207.184、连续 FIM 点同口径分数 234.433。基准单次运行约 5.08 s，启用连续 FIM 约 7.64 s（同一进程各运行一次，仅供量级参考）。曾误输入一次 `python -B -m ...` 导致命令行参数错误，随后使用正确命令完成全量测试。未执行 Git。连续 FIM 仍依赖有限边界场景和数值搜索，不构成原问题全局最优证明；项目状态保持【待验证】。
