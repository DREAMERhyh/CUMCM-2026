# AI 使用记录：Q2 双版本切换方案

## 输入

```text
现在期望将Q2新版和旧版功能都保留在当前代码中，Q2单独测试画图时默认开启新版，交给Q3Q4调用时也默认开启新版，但是使用一个参数让我们可以在测试启动之前更换调用版本，能做到吗
```

## 运行结果

完成双版本共存方案设计，尚未修改算法代码。建议以统一配置参数控制 Q2 圆域近似实现：默认使用新版自适应圆弧细化，可显式切换为旧版固定正多边形；参数同时贯穿 Q2 单独命令、Q3/Q4 策略构造、真实观测更新及滚动后验分支。现有规划入口保持兼容，仅增加带默认值的配置字段和关键字参数；待人工确认接口名称与取值后实施，并同步更新 Q2 API/README 与回归测试。
