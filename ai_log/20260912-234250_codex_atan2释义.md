# AI 使用记录：atan2 释义

## 输入

```text
5.2.6里(44)式里的atan2是什么
```

## 运行结果

核对了 `paper/paper.tex` 第 5.2.6 节式（44）。说明 `atan2(y-y_i,x-x_i)` 是双参数反正切函数，根据检测点到干扰源向量的纵、横坐标增量确定完整象限中的方向角；与普通 `arctan((y-y_i)/(x-x_i))` 相比，它能区分相反方向、处理横坐标差为零的情况。该式输出通常为弧度，可按需要转换并归一化到题目示向度的 `[0,360°)` 范围。此次仅作解释，未修改论文或代码。
