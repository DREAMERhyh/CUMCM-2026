# AI 使用记录：4-opt 新批次时间统计

## 输入

```text
再次处理新的4-opt/日志
```

## 运行结果

确认 `solution/output/sim/4-opt/` 当前为 5 个 02:01–02:06 生成的新 Q4 日志。沿用上一轮口径，将一个成功清除的信号源视为一个“点”：单点虚拟时间为最终累计 `virtual_time_s` 除以成功清除数，单点真实时间为退出记录 `elapsed_real_s` 除以成功清除数。

新批次 5 次运行等权平均为 973.086693 虚拟秒/点、2.245528 真实秒/点，共成功清除 72 点。按全部成功点合并加权为 953.821848 虚拟秒/点、2.256292 真实秒/点。单点虚拟时间样本标准差 191.746164 秒，真实时间样本标准差 0.227110 秒；失败清除率为 243 次清除尝试中的 171 次，即 70.370%；测量动作数/成功点为 23.791667。

相对上一批 4-opt 的 5 次等权均值，新批次单点虚拟时间降低 14.634%，单点真实时间增加 80.593%；失败清除率增加 17.340 个百分点，测量动作数/成功点降低 25.312%。第三份日志有 88 次失败清除，是本批失败率和波动偏高的主要来源。

5 个日志均检查为 HTTP 200、`accepted=true`、最终动作为 `exit`，累计虚拟时间单调不减。
