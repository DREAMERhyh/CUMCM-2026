# 输入

1. 80s是虚拟时间还是真实时间？虚拟时间上限100h，真实时间是20min 2. 我认为你的方案基本合理。回答：这会造成多少额外开销（真实时间，即CPU运算时间，不是机器狗消耗的虚拟时间）？以及参数lambda有什么调试方法（简述）？3. 命名为5%/10%近优域，其他按你的来 4. 按你的方案。这一步依旧只讨论方案

# 运行结果

本轮只讨论并记录方案，没有修改算法代码。已澄清此前约 80 秒是机器狗移动/检测的虚拟时间，不是 CPU 时间。现有粗测为单案例离散 baseline 约 5.08 s，baseline 加当前连续 FIM 约 7.64 s，增量约 2.56 s；200例16进程墙钟约134 s。估计共享缓存后的多预算 FIM、Pareto和在线局部5%/10%近优域会使单次规划总耗时约11–16 s；离线自适应高精度区域预计约20–40 s/案例，均需实现后专项实测，不能当作已验证数据。Q3/Q4应只用在线档，绘论文图再用离线档。

lambda建议先把半径和时间分别除以参考尺度，变成无量纲权重，再在训练场景上网格扫描，用清除成功率为硬约束、总虚拟时间为主指标，结合Pareto拐点选择，并在独立随机种子上做稳定性验证。优先采用相对baseline的硬时间预算，lambda只作敏感性或备选标量化参数。用户确认采用多时间预算FIM候选、集合复评分、Pareto筛选和按Q3/Q4预算选点；两个方案均输出确切点及“5%近优域”“10%近优域”，并在同图可视化。未执行 Git，项目状态保持【待验证】。
