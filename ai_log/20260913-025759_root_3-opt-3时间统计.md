# AI 使用记录：3-opt-3 时间统计

## 输入

```text
处理3-opt-3中的日志
```

## 运行结果

读取了 `solution/output/sim/3-opt-3/` 的 5 个 Q3 JSONL。沿用前述口径，将一个成功清除的信号源视为一个“点”：单点虚拟时间为最终累计 `virtual_time_s` 除以成功清除数，单点真实时间为退出记录 `elapsed_real_s` 除以成功清除数。

5 次运行等权平均为 405.151529 虚拟秒/点、29.354874 真实秒/点，共成功清除 62 点。按全部成功点合并加权为 399.624771 虚拟秒/点、29.708661 真实秒/点。单点虚拟时间样本标准差 42.431182 秒，真实时间样本标准差 12.940241 秒；失败清除率为 78 次清除尝试中的 16 次，即 20.513%；测量动作数/成功点为 8.774194。

与当前仍存在的 Q3 日志重算比较：相对 legacy，单点虚拟时间降低 13.133%，真实时间增加 216.920%；相对 new，虚拟时间降低 12.697%，真实时间增加 229.860%。原 `3-opt2/` 目录本轮已不存在，因此对 3-opt2 的比较采用上一轮已核验的 AI 日志汇总值：单点虚拟时间再降低 6.586%，真实时间增加 87.207%。

5 个日志均检查为 HTTP 200、`accepted=true`、最终动作为 `exit`，累计虚拟时间单调不减。一次尝试直接重读 `3-opt2/` 失败，原因是该目录已被移除，不影响本批 3-opt-3 统计。
