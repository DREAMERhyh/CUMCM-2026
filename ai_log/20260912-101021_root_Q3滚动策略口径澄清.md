# AI 使用记录：Q3滚动策略口径澄清

## 输入

```text
当前 Q3 默认使用的不是单纯 FIM，也不只是机械采用“混合顶层推荐”。

实际流程是：

1. Q2生成离散基线、连续FIM多预算解、Pareto候选和顶层混合推荐。
2. Q3把这些测点放入同一候选池。
3. 对候选模拟“移动＋测量＋27米网格清除＋前往下一信号源”的总虚拟时间。
4. 选择预计总时间最低的点，或者判断直接清除更好。

`prefer_continuous_fim=True` 只在关闭 `rolling_time_mode` 时生效；当前默认 `rolling_time_mode="scenario"`，因此不会固定执行FIM点。

我的建议：

- 在“当前滚动混合策略”和“单纯FIM”之间：选择当前滚动混合策略。
- 如果只能在“Q2顶层混合推荐”和“单纯FIM”之间选：选择顶层混合推荐。

原因是FIM只衡量测向信息质量，不能直接反映移动、清除和跨源路线的总耗时；混合方案还保留离散点作为安全后备。

相关代码位于 [policy.py (line 256)](C:/Users/30130/Desktop/数模/solution/src/q3/policy.py:256) 和 [rolling\_time.py (line 35)](C:/Users/30130/Desktop/数模/solution/src/q3/rolling_time.py:35)。需要注意：27米网格已接入滚动估值，但还没有重跑完整性能基准。
```

## 运行结果

已核对 Q3 默认滚动策略分支、候选池与 27 米网格估值链路，确认用户说明正确。`Q3Policy` 默认 `rolling_time_mode="scenario"`；联合规划和单源规划都会优先调用滚动评价，只有关闭滚动模式后才调用受 `prefer_continuous_fim` 控制的 `_refinement_candidate()`。滚动候选池包含 Q2 顶层推荐、离散基线、连续 FIM 选点及多预算解、Pareto 点、部分保证接收离散点和保证接收域代表点；评价计入移动、测量、27 米网格清除以及前往下一活动源的单步续程，并可返回直接清除。此前把 `_refinement_candidate()` 误判为默认主控制流的结论已纠正。27 米版本尚未重跑完整性能基准。本次未修改策略代码、项目状态文档或测试，未执行 Git 操作。
