# 输入

我同意你现在的联合方案。开始修改Q3

# 运行结果

已在 `solution/src/q3/` 实现虚拟时间优先的同点多频道联合批测，并保留原自适应策略作为 `joint_batch_mode=off` 基线。默认 `guaranteed` 模式只批测对整个后验区域保证接收、预计节省大于追加检测成本的其他活动频道；`all_active` 保留为激进离线对照。扫描阶段和扫描后联合规划均已接入，顺便测量不占专用 FIM 配额；Q4 显式关闭该功能以保持原四向策略。

新增 `src/q3/joint.py`、`tests/q3/benchmark_joint.py` 和两项联合批测单元测试，并为 Q3 CLI、官方模拟器策略入口增加 `--joint-batch-mode` 参数。固定种子 20260911 的 8 个 10–16 源场景中，三种策略均 8/8 完整清除；保证接收模式相对自适应基线平均虚拟时间由 7553.850 s 降至 6880.952 s，下降 8.908%，6/8 场景更快，P90 由 8546.249 s 降至 8085.280 s，因此作为正式默认。全部活动模式平均 6895.818 s、P90 9183.311 s，仅保留对照。结果写入 `solution/output/q3_offline/joint_benchmark.json` 和 `solution/test_res_q3_offline.md`。

Q3 离线测试 11/11、Q4 回归 4/4、全项目 80/80 通过，全量复跑耗时 111.515 s；收尾再次定向运行 Q3/Q4 共 15 项，0.926 s 全部通过，两个 CLI 均能显示新增参数。首次全量测试暴露 Q2 近优域可能按设计在 5 s 截止后返回 `partial`，原测试只接受 `ok`；已仅修正测试契约为 `ok/partial` 且 `partial` 必须标记 `timed_out=true`，未改 Q2 生产代码或参数。未执行 Git，未连接官方模拟器，Q1–Q4 状态保持【待验证】。
