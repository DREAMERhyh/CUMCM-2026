# AI 使用记录：性能演进考古（只读分析）

## 输入

```text
追加任务：性能演进考古（只读，不改任何代码与默认值）。
背景：晨间 5 局官方演练对账全过（数据我已发），但队员提出关键质疑：
"最初版本总虚拟时间约 6000s，凌晨 8 局（交错配置）实测 11000s+，
今晨 batch+A3 回到 5634~7663s——曲线是 V 形。中途是谁把 6000 改成
11000 的？早期离线推算与官方实测为何差距这么大？"
注意：今晨 5 局对账全过 + 同源数比凌晨快 15~51%，当前默认已是实测
最优，考古不改配置；目的是历史解释、推算文档修正、排除遗漏的更优
旧版本。正式测试在明天，今天完成即可。

数据（补全：局1 我未贴终端输出，请读文件补判）：
局1 q3_20260912-110952-324438 官方总数 11（其余字段读文件）
局2 q3_20260912-111451-146517 官方总数 15；cleared 15；动作 135；
    虚拟 7663.0s；均 510.9
局3 q3_20260912-111714-968811 官方总数 10；cleared 10；动作 133；
    虚拟 6749.0s；均 674.9
局4 q3_20260912-111828-755450 官方总数 12；cleared 12；动作 126；
    虚拟 7356.8s；均 613.1
局5 q3_20260912-111956-941118 官方总数 11；cleared 11；动作 136；
    虚拟 5634.4s；均 512.2
（5 局均 user_exit、时间对账差 ≤2μs）

请执行：
1. 先出今晨 5 局逐局判定表（cleared==官方 且 c+a==20），局1 补判，
   并追加进 drill_records.csv；同时核查：各局 fallback 触发次数是否
   为 0、guess_clear 各用了几次/命中几次/miss 几次、locate 段现实
   耗时（JSONL 首个 /clear 的 recorded_at_utc 相对扫描结束）与 20
   分钟窗口裕量——这是夜间两项翻默认的官方首次实测确认；
2. 考古任务 A——"最初版本 6000s"溯源：
   翻 ai_log/ 全部历史记录、项目文档、早期推算表，找出 6000s 这个
   数字的确切出处：是当时官方演练实测，还是离线推算？对应哪个
   配置（resolve 流程、refine 次数、有无 fallback 机制、布局）、
   什么源数规模、什么统计口径（总时间还是别的段）？
   如找不到实测记录，明确说"6000s 来自推算而非实测"；
3. 考古任务 B——V 形曲线归因：
   a) 列出从最初版本到 9-12 凌晨配置之间，resolve/清除流程的全部
      行为性改动（按 ai_log 时间线）：每项改动的动机、预期收益、
      对每源虚拟时间的实际影响证据；
   b) 重点验证假说：交错流程中"refine 不收敛 → strip fallback 链"
      是 6000→11000 膨胀的主因（夜报 exp5：交错 fallback 中位
      495~528 次/局；今晨官方实测若 fallback=0 则链条闭合）。
      用凌晨 8 局 JSONL 逐局统计 fallback 次数与耗时占比验证；
   c) 明确回答：是"某次改动把原本 6000s 的流程改差了"，还是
      "6000s 从未在同等条件下实测过（推算漏算了 fallback/refine
      开销）"。两者对论文叙事完全不同，必须分清；
4. 考古任务 C——离线↔官方一致性终检：
   simlite 对 batch+A3 的预测分布 vs 今晨 5 局官方实测（按源数分层
   对比；逐案例无法相同，比分布与机制）；夜间预测中位 -29% vs 官方
   实测同源数 -15%~-51%，解释偏差来源。给出"simlite 作为官方代理
   的可信度"最终结论，写进论文方法章节；
5. 结论模板（必须回答）：
   a) V 形曲线三段各自的解释（一句话一段）；
   b) 当前配置是否为全历史实测最优？（是→写"冻结依据"；若发现
      某旧版本实测更快→给出该配置与复验方案，交我裁决）；
   c) 早期推算文档/论文口径是否需要修正？给出修正条目；
   d) 论文性能演进小节草稿（6000→11000→6000 的机制解释，
      A3 guess_clear 与批量 TSP 如何消除 fallback 浪费）；
6. 以上全部为只读分析；唯一允许的写操作是文档/ai_log/CSV 更新。
   完成后恢复冻结状态等待我正式测试指令；如判定表出现任何对账
   失败（特别是局1），立即中止考古、按决策表归因并报告。

约束沿用：允许使用git相关操作，但是仅限此分支、不动协议层/幂等/certificate/fallback/near、
不引入概率分布假设、时间计算复用 time_model。
```

## 运行结果

- **判定表**：今晨 5 局 5/5 一致（cleared==官方 且 c+a==20，含局1 文件补判
  11/11）；已追加 drill_records.csv（13 行）。fallback=0、guess= 0 次（批量
  入口状态机属性，run_drill 日志无 mode 字段，以流程属性+clear miss 代理计数
  佐证：今晨 clear miss 为 0 源数级 vs 凌晨 350~879 次）；locate 现实消耗
  22.3~50.6s、窗口裕量 1149~1178s（修正夜报"3-5 分钟"推算为实测几十秒）。
- **6000s 溯源**：出处为并行轨道 commit 525ef5f（09-12 02:30）的
  test_res_q3_rolling_time.md：验证种子 4 场景"滚动时间全套"离线对拍平均
  **5873.934 s**（旧基线 7548.173 s），非官方实测；该轨道代码不在当前 HEAD。
- **V 形归因**：凌晨 8 局官方实录显示交错流程每局 350~884 次 clear miss、
  最长连续 miss 链 106~147（strip 保底链）——6000 从未在同等条件下实测过
  （推算漏算/流程不同轨），不存在"某次改动把 6000 改差"。
- **一致性终检**：simlite batch+A3 预测按源数分层中位 vs 今晨官方 5 局：
  官方全部落在预测包络内（5/5），中位偏差 -15%~+6%（层样本 1-2 局）；
  配置间差距（simlite -29% vs 官方同源数 -16%~-48%）同向。
- **文档修正**：PROJECT_CONTEXT 追加考古修正条目；q3教学/06 加"6000~7000m
  为移动路程米数"防误读注；夜报 locate 推算修正写入考古报告。
- **产出**：docs/性能演进考古_20260912.md（含结论模板 a-d、论文小节草稿、
  凌晨 8 局实录表）；未改任何代码/默认值；无 Git 写操作（仅只读查询）。