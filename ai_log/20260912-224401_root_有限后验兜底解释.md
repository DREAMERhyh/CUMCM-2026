# 输入

有限后验失效或超时  这是什么？

# 运行结果

解释了 Q4 中“有限后验失效”和“滚动计算超时”的含义：前者是离散采样场景在连续真实可行域中没有保留下可用于评分的样本，后者是单次滚动估值超过默认 1 秒真实墙钟软截止。二者都只会停止启发式选点并回退到具有连续几何覆盖保证的 27 m 清除网格，不表示信号源消失或策略终止，也不会使用有限样本作为不漏清证明。未修改业务代码或项目状态。
