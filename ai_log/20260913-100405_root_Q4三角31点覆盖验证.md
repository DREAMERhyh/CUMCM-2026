# AI 使用记录：Q4三角31点覆盖验证

## 输入

```text
先验证31点网，如果保证可以覆盖，向我确认。
```

## 运行结果

对 `q4.directional.triangular_scan_mesh(spacing=901.0, arena_radius=1800.0, min_receive_radius=1000.0)` 做了理论与数值双重覆盖验证，未修改策略代码、测试状态或人工验收状态。

理论上，边长 901 m 的等边三角网铺满平面，生成器选择所有与半径 1800 m 目标圆相交的三角形并纳入其全部顶点。圆内任意源点属于至少一个被选三角形；源点到该三角形任一顶点不超过三角形直径 901 m，小于最小接收半径 1000 m。源点是三个顶点的凸组合，对任意过源点的闭发射半平面，至少一个顶点位于该半平面内，否则三个方向点积全负会与凸组合为零矛盾。因此对全向源及当前“前向闭半平面”定向源模型，只要每个未决频道完整访问31点，就存在至少一个可接收扫描点，覆盖保证成立，接收距离余量为99 m。

实现核验得到31个唯一点、42个三角形；把格点索引独立扩大到 `[-20,20]` 后重新枚举，与生成器选出的42个相交三角形完全一致，无漏枚举。对圆内5 m笛卡尔网格加0.1度圆周边界共410999个位置逐点检查，均落入至少一个被选三角形，包含三角形的最大顶点距离为901.000000 m。另以固定种子检查300000组随机源位置与发射方向，全部存在1000 m内可见点，样本最坏最近可见距离约900.247358 m。路径从原点开始，共31个唯一点、30段，每段恰为901 m，总长27030 m，纯移动5406 s，最远点半径约2383.822 m。所有断言通过，计算约15.6 s。该结论是当前数学模型和本地实现下的理论保证与自动核验，不等同于人工或官方模拟器【已验证】。
