# 输入

时限增加到6s。我会准备在线测试Q3，你可以修改其中离线测试的配置来适配在线测试

# 运行结果

已把 `Q3Policy` 的单次连续 FIM 真实墙钟上限从 4 s 调整为默认 6 s，并把该参数开放给 Q3/Q4 策略和正式 `sim/cli.py` 的 `--fim-cpu-time-limit-s`。Q3 离线 CLI 新增 `--max-actions`、`--max-refinements` 和该时限参数，默认改为与在线策略相同的两次细化和 6 s，不再用零细化绕过 Q2。端到端离线测试改为计数两次真实细化规划，并保留接口专项测试确认 Q3 消费 Q2 顶层混合推荐点、关闭近优域。同步更新 Q3、模拟器说明、人工验证清单、离线测试结果和项目上下文。

检查结果：Q3 专项 7/7 通过（2.294 s）；默认离线 CLI 成功执行 140 个动作、两次细化、清除频道 3，虚拟时间 2959.614 s，并报告单次 FIM 上限 6.000 s；Q4 继承参数后 4/4 通过；全项目 76/76 通过（66.071 s）。正式 CLI 的本地门禁检查在缺少 `--confirm-ready` 时按预期退出，未创建客户端、未连接模拟器。一次修改前诊断用旧 4 s 配置跑通两次细化，仅用于确认离线路径可终止；最终结果均基于 6 s 配置。全程未执行 Git，未连接官方模拟器，状态保持【待验证】。
