# 输入

现在可以进行q3/q4的模拟吗，有没有非官方的仓库自行实现的模拟

# 运行结果

只读检查了 `solution/src/sim`、Q3/Q4 离线入口、测试与说明文档。确认仓库自行实现了 `sim.fake.FakeSimulator`：支持全向/定向源、距离接收、近距离提示、测角误差、20 m 清除、动作虚拟时间和 request_id 幂等，可供 Q3/Q4 离线策略运行，但不是官方模拟器。

Q3 的 `src/q3/cli.py` 提供单场景端到端演示，并有多源随机场景和策略配对基准；Q4 的 `src/q4/cli.py` 也可端到端运行定向源，但目前只有固定单源演示和基础随机覆盖/状态机测试，策略文档仍标记为算法原型，仿真成熟度低于 Q3。

实际执行 `python -B -m unittest discover -s tests/q3 -t . -v`，32 项全部通过；执行 `python -B -m unittest discover -s tests/q4 -t . -v`，4 项全部通过。未连接、未运行官方模拟器，也未据此改变任何题目验证状态。
