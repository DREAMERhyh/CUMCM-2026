# AI 使用记录：夜间自主优化（Q3 全流程，无人工确认模式）

## 输入

```text
【夜间自主优化任务：Q3 全流程，无人工确认模式，现在开始】

背景与状态更新（覆盖此前所有待确认事项）：
1. 8 局演练对账已完成：局4-8 连续一致，pure_ring8 布局锁定；
   一致性口径已修正为 cleared == 官方总数 且 c+a == 20（采纳你的修正）；
2. 局1-3 是动作上限（1000）触发、非覆盖缺陷；你已把 Q3 默认
   max_actions 提到 8000 —— 该修复视为"临时生效、待晨间官方演练验证"；
3. 分工变更：Q4 全部工作由另一位队员负责，不在你的任务范围内
  （后续若收到 Q4 程序级 bug 支援请求，优先处理，但今夜不会有）；
4. 我现在开始睡觉，约 8~9 小时无法回答任何问题。这段时间你获得
   完全自主权：自主设计实验、自主运行测试、自主分析数据、自主决定
   优化项是否晋级为默认配置，无需任何人工确认。唯一不可用的资源是
   官方模拟器（需人工 GUI 操作）——今夜一切验证用 simlite，官方
   演练验证统一排队到晨间清单。

【开始前先做两件事】
1. 读 AGENTS.md、ai_log/ 最近 3 条记录、相关模块现状；
2. 创建并持续维护进度状态文件 ai_log/夜间自主优化_进度.md：
   每完成一个阶段原子更新（已完成项、决策、证据路径、下一项、
   未决问题）。若会话中断，新会话必须先读该文件从断点继续，
   不重做已完成的工作。

【自主权边界（是正确性边界，不是时间约束，任何情况下不得突破）】
- 不动：HTTP 协议层、request_id 幂等、certificate/fallback 保底、
  near 逻辑、sim/ 接口层语义（max_actions 修复已做，不再动第二次）；
- 不引入任何概率分布假设（误差模型是有界系统误差；simlite 的场景
  随机性属于评估采样，不得进入策略设计）；
- 不做 Git 操作；所有时间计算复用 common/time_model.py；
- 今夜不碰官方模拟器与其账号；
- 并行计算留 2 个核给系统。

【门1：simlite 自主验收标准（全部通过才允许翻默认值）】
固定 seed 池同池成对对比（两臂跑完全相同的案例）：
a) 逐局 cleared 不得低于基线臂（任何一局清除数下降即不通过；
   出现上升必须逐局解释）；
b) 逐局 c+a == 20、absent 判定完整；
c) fallback 触发次数与 clear 失败尝试次数总量不得上升；
d) 总动作数 ≤ max_actions 且留 ≥30% 裕量；
e) 总虚拟时间：改进局数 > 恶化局数 且中位数改进 > 0；
   逐局最坏恶化幅度必须报告（即使整体通过）；
f) 全项目 unittest 全绿 + git diff 核验禁改区零触碰。
通过 → 翻默认值并写决策日志（项名、证据表路径、回滚命令一行）；
不满足或模糊 → 保持默认关闭、记录原因、进入下一项。
晋级逐项进行（一次只翻一项，翻完跑全量回归），保证可归因。

【阶段0：吞吐自校准（最先执行）】
实测 simlite 单局墙钟（含规划耗时）与 CPU 核数，据此定今夜每臂
可行样本量：筛选实验 ≥100 局/臂、晋级确认 ≥300 局/臂（达不到就
报告实际达成量及原因）。样本量不够时优先保证靠前的阶段做足，
后面的进入"后续会话队列"（写清完整计划，任何项都不删除、
都算排队而非砍掉）。同时输出：动作数分布（重点 14-16 源局）与
8000 上限裕量、单局 JSONL 尺寸估算。

【任务管线（按序执行，每完成一项更新进度文件）】
阶段1 E1 基建：replay_batch 升级为成对比较框架——逐局明细
 （各阶段虚拟时间、每源测量次数、fallback 次数、clear 尝试
 次数、总动作数）+ 汇总统计（中位/均值/P90/逐局差值分布）
 + 决策日志与回滚登记机制。验收：ring7 vs pure_ring8 同池
 ≥300 局对比报告（论文数据 + 验证 460s 离线推算是否兑现）。
阶段2 A1+A6 排除圆修剪：仅全向源（代码与注释显式标注前提）；
 仅"已被 direction/near 确认有源的频道"的 no_signal 才追加
 排除圆 B(测点,1000)；扫描期 no_signal 只用于 absent 判定；
 区域 = 凸多边形 + 排除圆列表（保凸，不做布尔差）；
 worst_radius 口径改为"可行代表点最坏距离"并文档化。
 开关 use_no_signal_pruning 默认 False。门1 验收。
阶段3 A2+A5 目标统一：q2 连续优化主目标换为 worst-case 直径
 （复用 _posterior_radius 口径、排除圆感知），FIM 降级为
 粗筛/初值；模式搜索升级为多起点 basin hopping（16 随机重启，
 anytime 预算兜底）。然后【重开】搜索深度实验：新目标下迭代
 120/600/3000，口径改为端到端总虚拟时间——此前"+0.02% 关闭"
 仅在 FIM 目标下有效，本次以新证据为准；若仍 <1% 则再次关闭
 并归档（这次关闭理由是证据，不是时间）。A2 与深度结论分别
 记录，A2 走门1。
阶段4 A3 measure/clear 最优停止：确定性期望成本模型（对最坏
 误差组合取值）：再测成本 = 移动+5s+切换，收益 = 最坏组合下
 测后区域直径缩减 × 后续时间折算；直接猜 clear 成本 = 移动+3s；
 区域直径 ≤40 时猜中心点有覆盖保证。接入 _resolve_action，
 max_refinements 退化为上限保险。门1（重点看 c、e 两条）。
阶段5 B2 流程变体实验：交错（现状）vs 批量解耦（全部定位后
 Held-Karp n≤16 精确 TSP 规划清除顺序）。同池对比出裁决；
 解耦显著更优才走门1，否则保留代码 + 论文结论"交错优于解耦
 及原因分析"。
阶段6 C2 扫描变体（扫到 direction 立即处理，需验证 absent
 判定完整性不受影响）与 D1 动态调度（按剩余期望成本贪心重排
 源处理顺序，复用 A3 成本模型）。各自开关、各自门1。
阶段7 E3 参数敏感性（error_deg、MEC 阈值、max_refinements、
 A3 折算系数；网格规模按吞吐自定）输出响应面与推荐参数组
 （推荐参数翻默认同样走门1）；B3 fallback 网格自适应（带间距/
 带数/方向按区域形状调整），门1 重点看 fallback 触发局的
 清除尝试次数。
阶段8 论文层（不改运行时代码）：A4 第三测点交会角解析近优域
 分析小节；C1 覆盖-TSP 联合优化与覆盖数下界讨论；C3 频道切换
 顺序最优性证明（一行）；E2 最坏情况保证 vs 蒙特卡洛平均
 对比图。各自成草稿小节 + 图表数据。

【收尾闭环（必做，即使部分阶段未完成）】
1. 最终组合验证：夜间最终默认配置 vs 晨间锁定基线
 （pure_ring8 + max_actions 8000 + 其余新开关全关），同一
 seed 池全量对比：cleared 逐局不降、总时间统计、逐局最坏
 恶化报告；
2. 汇总报告写入 docs/夜间自主优化报告_20260912.md 并在最终
 回复中完整展示：执行摘要 + 决策表（翻了几项默认、各自证据
 与收益、回滚命令）；各阶段统计表与证据文件路径；吞吐与实际
 样本量说明；晨间人工清单；后续会话队列（未完成项完整计划）；
 开放问题与风险；
3. 晨间清单（写进报告供我醒来执行，给出精确命令行含占位符）：
  a) 用当前默认配置跑 1~2 局 13+ 源 Q3 官方演练，确认 8000
     动作上限不再触发（验证 max_actions 修复）；
  b) 对夜间每一项翻默认的优化，跑 3~5 局官方演练对账
    （cleared == 官方总数 且 c+a==20），任何异常按决策表
     回滚命令一键回退。

【工作纪律】
- 遇到无法解决的阻塞：记入进度文件"未决问题"，跳到下一个
  独立项，不空转；
- 遇到歧义：选保守选项，在报告"推迟决策"一节说明；
- 中间回复保持简短，细节写文件；每阶段写 ai_log，
  AGENTS.md 约定全程遵守；
- 今夜一切结论以 simlite 证据为准，最终上线以我晨间官方
  对账为准。

如果你有更好的想法，或者找出了我的需求里面有问题的地方，我给你特权，你可以自行按照你认为正确的方式进行工作，现在开干吧！
```

## 运行结果

见 `ai_log/夜间自主优化_进度.md`（逐阶段原子记录）与
`docs/夜间自主优化报告_20260912.md`（最终报告，交付消息中完整展示）。
执行摘要：翻默认 2 项（A3 最优停止、B2 批量入口），拒绝 2 项（A1 行为中性、
A2 现实成本），E3 安全边界（error_deg 须 ≥1°）；全部证据 120+300 局同池
成对对比（门1）；最终组合验证 150 局门1 全过（总虚拟 -29%）；全量
unittest 147/147 通过；禁改区零触碰；无 Git 操作。