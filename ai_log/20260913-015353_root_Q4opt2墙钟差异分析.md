# AI 使用记录：Q4 opt2 墙钟差异分析

## 输入

```text
现在我开启了Q4的opt2，但是wall-time显著短于Q3，为什么？
```

## 运行结果

核对 Q3/Q4 当前策略调用链后确认：假定 opt2 指 `multi_source_route_mode=insertion_2opt`，Q4 墙钟短于 Q3 属于预期。Q3 在路线选择前会对所有可细化源构造 Q2/FIM 规划并做滚动评价，且没有单源服务锁，会在多个真实响应后再次进入路线选择；Q4 路线规格只使用已有后验清除点，不调用 Q2/FIM，并以 `route_active_channel` 锁定选中源直至清除，路线通常每源只规划一次。3 s 滚动和 10 s FIM 均为软上限而非固定等待时间。未修改代码或执行在线请求。
