## 输入

```text
不考虑A题了，探索一下B、C题潜在的算法
```

## 运行结果

完成 B、C 候选算法探索，并执行几何覆盖检查及按时间顺序的预测基线比较；未运行官方模拟器、未消耗正式机会、未生成最终结果工作簿。诊断脚本位于 /tmp/bc-algorithm-explore-BhmzjR/probes.py，原始题目及附件未修改。

B 题从题设推导得到：原点加半径 900√3 米的正六边形六顶点，共 7 个检测点，对半径 1800 米的目标圆域有最大最近距离 900 米的覆盖保证，低于最小接收半径 1000 米。定向源可采用边长 700 米、坐标为 -2100 至 2100 米的 7×7 方格顶点；目标位于某网格单元内，四顶点均不超过 700√2 米，且任何过目标的发射半平面包含至少一个顶点。进一步构造边长 900 米的三角网格，保留所有与目标圆域相交的三角形的顶点，本次得到 37 个点（不是最少点数结论，最外节点半径 2700 米，题设允许区域外检测）。三角形内任意目标距三顶点不超过 900 米，凸组合关系保证任意发射半平面至少含一个顶点。对 50000 个随机域内点、7201 个边界点及 4 个特殊点，共 57205 点进行了检查：7 点全向覆盖最近距离最大约 900 米；49 点方格与 37 点三角格的近邻方向最大角隙均不超过 180 度，抽查无漏覆盖。抽样不是连续覆盖证明，保证依据上述几何关系。

B 题定位候选为角域约束交集、凸多边形直径、最小包围圆、自适应检测点选择及按单位行动时间衡量的信息增益；路线可用最近插入和 2-opt。补充有限光学清除兜底：一次有效示向度配合 1500 米接收上界，可把目标包在局部长 1500 米、半宽 1500 sin(1.01°) 米的矩形内；按纵向 54 格、横向 2 格分割，在 108 个格心清除，最大覆盖距离约 19.18 米，小于 20 米。1.01 度用于覆盖题设误差与两位小数读数的数值裕量。该兜底独立于目标的发射方向；普通策略应优先少量测向后直接清除。上述证明不等于官方模拟器联调成功或时间最优，现实运行时间和日志体积仍需测试。

C 题候选为分项预测、分位数备用、LP 或 MILP 储能调度、情景优化与滚动控制；进阶可比较 CVaR 或预算鲁棒优化。情景模型需要处理非预见性，不能把每条情景的未来真值视为提前已知。第一问可写为 720 个连续变量、289 个等式约束的 LP 基础形式。尝试运行时发现 bundled Python 缺少 SciPy；保留模型构造但跳过求解，没有安装依赖，也没有得到或声称得到最优费用。几何检查改用 NumPy 显式构造三角网格，预测检查继续完成。

C 附件 2 的 2025 年 2 月至 12 月共 334 天、每天 144 点顺序回测：负载 MAE（kW）为昨日同期 650.02、上周同期 176.80、过去 7 日同期均值 781.21；光伏 MAE 分别为 185.35、214.69、153.40。上周同期负载减去过去 7 日光伏均值的净负载预测 MAE 为 273.35 kW。所有上述基线在每天预测时仅使用此前的观测，无训练过程或未来输入。

C 附件 3 的更新预报与当日 0 点预报按相同目标整点比较，评价区间同为 2 至 12 月：6 点更新在 7 至 24 点共 6012 点的 MAE 为 129.09 kW，0 点版本为 247.97；12 点更新在 13 至 24 点共 4008 点为 35.20，0 点版本为 191.18；18 点更新在 19 至 24 点共 2004 点为 0.000358，0 点版本为 0.006196，实际光伏最大仅 8.7375 kW。比较按小时目标对齐，不含 18:00 至 19:00 内的非整点信息，不能据此认定 18 点预报在全年经济调度中无价值。不同更新时刻的评价时段不同，误差不能直接横向排名；预报精度改善也不等于费用改善。

由题设推导一个简化经济解释：无储能、单时段、剩余购电无残值且普通与紧急电价为 p 和 5p 时，目标 p q + 5p E[(N-q)+] 对应净负载的 80% 分位数采购；不能直接作为带储能全天模型的最优规则。第三问需核对下调购电的退款与违约费用口径，否则原计划全额付费且额外罚款时，在可弃置余电条件下下调无经济收益。全年参数选择与最终策略评价须分开，不能用全年探索统计替代独立回测。

参考资料为 CGAL 最小包围圆官方文档、SciPy linprog/milp 官方文档、scikit-learn 时间序列与分位数回归示例、Stanford 储能滚动控制研究及 Bertsimas–Sim 预算鲁棒优化论文。最终建议优先试验 B 的覆盖保证加自适应定位组合，或 C 的周周期负载预测、分位数备用和滚动 LP 组合；不把强化学习、遗传算法等作为首个方案。
