# 输入

在当前文件结构下进行部分重构：将Q1相关的代码放在problem_b/q1/中，并且修改其他代码对其函数的调用，保持所有代码的可运行性。之后修改Q2的代码，保证完成题目要求

q1迁移到problem_b/code/q1，可以清楚code之外的代码，尽量保持code/内的代码按题目分布。你可以修改q234中的相关函数的引用路径。

# 运行结果

已将Q1正式算法、CLI和绘图统一迁入 `problem_b/code/q1/`，将确定性用例生成与独立对拍工具归入 `code/q1/testbench/`，删除 `code/` 根部的旧兼容入口，并修正Q2、Q3、Q4、旧合成环境、自动化测试和说明文档中的引用路径。

Q2新增连续保证接收域（1000米圆交集的安全内近似）和连续可能接收域（1500米圆盘Minkowski和的保守外近似），保留有限离散候选评分，新增稳定候选编号、动作时间分解、JSON字段和区分源位置域/探测器候选域的可视化。固定示例生成46个离散候选，其中12个保证接收，选择点约为 `(4.172, -96.457)`。

Q2专项9项测试全部通过，Q1至Q4全量52项测试全部通过（35.674秒）；Q1小规模18例独立对拍完成480项检查、0失败；Q3/Q4离线CLI与旧浏览器JavaScript语法检查通过。新版Q2 PNG已目视检查，连续域、离散候选、候选编号、图例和坐标均清楚。

测试期间发现Windows GBK控制台不能输出上标字符 `²`，已改为 `m^2` 并复测通过。当前未使用Git，Q3/Q4仍未接入官方模拟器，不能把离线结果视为正式成绩。
