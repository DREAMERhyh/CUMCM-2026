# AI 使用记录：Q3Q4在线路径未使用代码审计

## 输入

```text
# Q3调用新版Q2，开启opt2路径优化

python src/sim/cli.py --problem 3 --mode policy --robot-id "202601101010" --q2-version new --max-refinements 5 --fim-cpu-time-limit-s 10 --joint-batch-mode guaranteed --failed-clear-remeasure-mode gated --rolling-time-mode scenario --rolling-risk-metric cvar --rolling-cpu-time-limit-s 3 --multi-source-route-mode insertion\_2opt --route-cpu-time-limit-s 3 --confirm-ready --confirm-policy

# Q4调用新版Q2，开启opt2路径优化

python src/sim/cli.py --problem 4 --mode policy --robot-id "202601101010" --q2-version new --max-refinements 2 --q4-scan-mode triangular37 --failed-clear-remeasure-mode gated --rolling-time-mode scenario --rolling-risk-metric cvar --rolling-cpu-time-limit-s 3 --q4-long-clear-tail-mode adaptive --multi-source-route-mode insertion\_2opt --route-cpu-time-limit-s 3 --confirm-ready --confirm-policy现在使用这两条进行测试。告诉我现在solution/src/中有哪些文件/函数/功能是不被使用的？
```

## 运行结果

按两条在线命令从 `src/sim/cli.py` 进入后的实际可达路径进行了只读静态审计，未连接或操作官方模拟器。首先确认命令若在 PowerShell 中原样包含 `insertion\_2opt` 会被参数解析拒绝，实际应写成 `insertion_2opt`；后续审计按无反斜杠的预期参数进行。

两条命令均不执行的整类代码包括：`src/legacy/`、`src/runtime/`、`src/tools/log_visualize/`，Q1/Q2/Q3/Q4各自的离线或绘图 CLI（`q1/cli.py`、`q1/bearing_plot.py`、`q2/cli.py`、`q2/plot.py`、`q3/cli.py`、`q4/cli.py`），以及本地替身和 smoke 行为（`sim/fake.py`、`sim/smoke.py`）；后两者可能因包或顶层导入而加载，但 policy 模式不会实例化执行。`src/` 中所有 Markdown、README、教学文档和前端静态文件也不参与运行。

参数明确关闭或绕过的功能：Q3/Q4均关闭有界计算缓存及 `beam_cached` 束搜索，`cache-capacity`、`beam-width`、`beam-max-expansions` 对这两次运行无效果；Q3关闭旧版Q2、近优域、旧半径代理判停、条带清除、联合批测的 `off/all_active` 变体和路线 `off/beam_cached` 变体；Q4关闭Q3联合批测、Q3滚动总时间和Q3的Q2测点规划，改走Q4方向联合后验、认证探测点和方向滚动，故Q4并不执行Q2离散候选、连续FIM或Pareto，仅使用 `q2_version=new` 的后验区域几何口径。Q4的 `grid121/custom` 扫描、滚动关闭时的自适应四侧探测、长尾救援关闭模式和路线关闭模式也不走。

函数级确认：`q2.planner.plan_second_point`、`q2.near_optimal.local_sample_points/build_near_optimal_regions`、`q1.geometry.localize/clip_to_box/diameter_bruteforce`、`q3.adaptive.radius_clear_cost_proxy/measurement_is_worthwhile`、`q3.coverage.strip_clear_points/nearest_coverage_distance`、`q3.route.plan_beam_cached_route/remaining_cost_lower_bound`、`q4.directional.grid121/adaptive_four_sided_points/four_sided_points` 不在这两条在线路径中。`q3.route._movement_segments` 在整个 `solution/` 中没有调用；`nearest_coverage_distance` 与 `four_sided_points` 仅被测试使用。其余许多私有辅助函数属于上述未运行整文件，未逐个重复列名。未修改算法代码，未运行测试。
