# AI 使用记录：Q2 选点策略与候选点图

## 输入

```text
继续修改：
1. 5.2.5/5.2.6 的两个方案完全没有阐明是什么的方案，应明显地写出是在候选区域里选择候选点的策略
2. 5.2.5 说“离散基线的工程综合分数”，过于抽象，改为有限离散候选集的综合评分，\lambda_R 不要说什么工程综合分数，可以改为超参数
3. 5.2.5 候选点应作图示意，可以参考图六和图五绘制
```

## 运行结果

修改了 `paper/paper.tex` 的 5.2.5、5.2.6 及相关概述和求解流程。新增过渡段，明确两种方案均用于从第 5.2.3 节候选区域中确定第二检测点：方案一是在可能接收候选区域内生成有限离散候选点、优先筛选保证接收点并进行综合评分；方案二是在保证接收候选区域内连续改变坐标，以 Fisher 信息指标产生候选点。两节标题和首段均同步改写。

将“离散基线的工程综合分数”统一改为“有限离散候选集的综合评分”，公式标签改为 `eq:q2-discrete-score`；将 `lambda_R` 定义为平衡行动时间和定位不确定性的权重超参数，并在符号表补充其单位和含义。同步把“离散基线”改为“方案一选点”。根据实际 `generate_candidates` 实现修正了特殊点表述：代码加入的是首次检测点与最小包围圆中心的中点，以及保证接收内近似的顶点均值中心，不包含源区域中心本身。

修改 `paper/code/q2_region_figures.py` 并新增 `paper/figures/q2-discrete-candidates.pdf` 与同名 PNG。新图沿用现有区域图和场景图配色，左图展示可能接收域、保证接收内近似、源位置区域与全部候选点，右图分类展示 20 个前向横向点、24 个中心环状点和 2 个特殊点；红色空心圆标出保证接收内近似中的 12 个候选点。绘图程序核对 46 个示意点与实际 `generate_candidates` 输出完全一致。同步更新 `paper/README.md` 的绘图脚本说明。

图件为有效单页 PDF，并用 Poppler 以 180 dpi 重新渲染后目视检查，未发现文字、图例或坐标裁切。执行静态 LaTeX 检查，82 个环境起止、43 个标签与引用、8 个插图路径、3 个参考文献键均正常；旧表述“工程综合”“离散基线”和旧公式标签无残留。当前环境没有 XeLaTeX，未生成整篇论文 PDF，仍需在 Windows 宿主机编译检查分页、图号和交叉引用。
