# AI 使用记录：Q2 双版本切换实施

## 输入

```text
按这个要求修改
```

## 运行结果

已在当前 `solution/` 同时保留 Q2 新旧源区域算法：`new` 使用自适应端点/超差切线细化，`legacy` 使用原固定外切正多边形。`Q2Config.q2_version` 和 Q2/Q3/Q4/现场 CLI 的 `--q2-version new|legacy` 均默认 `new`；参数已贯穿 Q2 测后评分、Q3 实际观测更新、Q3 滚动测后分支和 Q4 继承路径，且版本进入缓存配置指纹。同步更新 Q2 API/README、Q3/Q4/sim 文档、教学说明、快速上手和 `PROJECT_CONTEXT.md`。

验证结果：双版本定向组合 65/65 通过；新增 Q4 在线入口检查后全项目 `python -B -m unittest discover -s tests -t . -v` 为 127/127 通过，耗时 85.924 s；额外核对四个 CLI 帮助和真实函数签名，Q2 旧版分支与手工构造的固定外切半平面顶点完全一致。未连接官方模拟器，Q1-Q4 状态保持【待验证】。
