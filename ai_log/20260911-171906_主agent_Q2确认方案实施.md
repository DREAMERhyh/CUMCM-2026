# 输入

方案确认，开始更改

# 运行结果

已完成 Q2 分时限连续优化重构：保留离散基线，在相对基线多 15/30/60 秒的虚拟动作预算下搜索连续 FIM 候选，全部用原有界误差集合评分器复算 `R_wc` 与 `T`，输出 Pareto 前沿，并在默认最多多 30 秒的执行预算内按“半径优先、时间次之”选连续点；顶层安全裁决在离散点与连续点之间给出 Q3/Q4 使用的确切推荐点。新增真实 CPU 墙钟截止和近优域 `off/online/offline` 档位；两个方案均输出 5%/10%近优域的采样凸包、面积和局部坐标范围。图中同时显示两个确切点、最终推荐点、近优域及局部放大、两套综合分数和 `T-R` Pareto 前沿。Q3 已改为调用顶层混合点并关闭绘图采样，Q4 继承。

验证结果：Q2 单元/CLI 9/9 通过；随机、边界、对抗对拍共 12/12 通过；固定种子 200 例中，连续方案按最坏后验半径 200/200 优于离散基线，均值 119.037 m 降至 70.082 m，按 `T+0.5R` 则 76/200 更优，平均多 29.706 s 虚拟动作时间，16 进程墙钟 122.015 s。单例 CPU 粗测为仅离散 3.741 s、FIM无近优域 4.115 s、在线近优域 6.919 s、离线近优域 17.227 s。全项目 76/76 测试通过，Q3/Q4 离线测试通过；图形改动后 CLI 测试再跑 1/1 通过。生成 `solution/output/q2_pareto_regions.{png,json}`，并更新 Q2 API、README、人工清单、比较报告、测试结果和项目上下文。近优域仅验证采样点，凸包内部不是连续证书；未连接官方模拟器，Q1-Q4 仍为【待验证】。实现中曾发现并修复多预算结果字段不一致和测试适配错误。全程未执行 Git。
