# AI 使用记录：Q4 在线测试说明

## 输入

```text
q4怎么进行在线测试
```

## 运行结果

核对了 `solution/src/q4/README.md`、`solution/src/sim/README.md`、`solution/tests/sim/verify_manual.md` 与 `solution/src/sim/cli.py`。结论：Q4 在线测试须由队员先在官方模拟器中打开“问题4演练测试”并等待接口就绪，再从 `solution/` 使用 `src/sim/cli.py --problem 4`；先做三动作 smoke，核对终端、JSONL 和模拟器反馈一致后，才可人工决定是否运行带 `--confirm-policy` 的完整原型。现场 CLI 默认连接 `http://127.0.0.1:2026`，完整 Q4 默认最多 4000 动作并保留 15 秒退出余量。

已执行本地检查：模拟器通信层 23 项测试全部通过；Q4 离线 4 项测试全部通过；不带 `--confirm-ready` 的 Q4 命令按设计拒绝执行且未发送请求。未连接或操作官方模拟器，在线结果仍需队员人工验证。
