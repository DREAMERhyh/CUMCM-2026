# AI 使用记录：实验轮3 全速压榨（地板核验 + 架构演进 + 顶配组合）

## 输入

```text
【实验分支·全速压榨终版：唯一目标=尽可能低的均时/源，完全自主】

━━━ 背景与授权 ━━━
当前分支为纯实验分支，论文已可上交——没有任何保守理由，唯一任务
是把均时/源压到极限。当前成绩：450s/源平均（官方口径，实验分支）。
Git 政策：只允许当前分支内操作（commit/branch/tag 限当前分支），
禁止影响其他分支与 main；回滚命令一律写成当前分支内一行形式。
无人工确认。你有全部决策权：方案取舍、可行性判定、实验设计、
架构重写，全部自己定。

━━━ 参照系：地板（先核验，你有权推翻但须用数学）━━━
- 扫描覆盖下界：8 停点×20 频道、移动 6103m → ~2160s；
- 清除环游下界：BHH 定理 L≈0.7124·√(N·π·1800²) ÷ 5m/s
 （N=10/13/16 → 1440/1640/1820s）；
- 合计地板均时：N=10/13/16 ≈ 390/318/270 s/源。
你的任务0：独立推导你自己的地板表（覆盖巡回、检测/切换、残差
定位各项都算清楚），一切结果按 N 档对账"实测 vs 地板"，剩余
差距必须给出"可收割/不可收割"论证。若你证明某数字不可达，用
数学证明它；若可达，用实验证明你做到了。
压力条款：若最终结果 >350s/源，报告必须用数学证明剩余差距属于
不可收割地板——"设计未穷尽"四个字我不接受，要么做到，要么
证明做不到。

━━━ 正确性底盘（防崩坏，非保守）━━━
- 门1 六条：同池成对对比 a)cleared 逐局不降 b)c+a==20
  c)fallback/clear失败不升 d)动作≤上限且≥30%裕量
  e)时间改进局>恶化局且中位>0 f)全量测试绿；
- 逐局最坏恶化必须报告（即使整体通过）；
- 禁改最小集：官方模拟器与账号、sim/ 协议层语义、时间对账口径
 （time_model）、absent/证书/保底的确定性语义；其余一切代码
 ——策略骨架、规划器、布局、扫描流程、清除流程——全部在改
 授权内，包括推翻你自己昨晚的设计；
- 不引入概率分布假设：策略保证逻辑全部确定性最坏情形语义；
  RL 的训练机制不受此限，但其动作必须经确定性校验器裁决；
- 样本量自定（建议筛选≥120/臂、关键对比≥300/臂），极端压测
 （N=16、贴边源、R=1000 最坏场景）必做；
- 进度文件续写 ai_log/夜间自主优化_进度.md（断点可续）。

━━━ 方向池（全部由你自主裁决：理论推导→预实验→取舍）━━━
以下方向可行性验证全部交给你——先写几何/规则论证，再做小样本
预实验，正收益才进同池大实验；零收益也归档数据（失败归因是
论文金子）。你可以推翻任何一条的预设、可以合并、可以自己新增
至少 3 条自己的设计。

A. TSPN 清除邻域（注意外部方案的几何错误修正版）：
   可行清除集 = { p : 区域内任意可行源位置到 p ≤ 20 }
              = ball(MEC圆心, 20−r)，邻域半径是 20−r 不是 20
  （r=19.9 时仅 0.1m 必须打圆心；r 越小邻域越大）。Held-Karp
   距离矩阵改为圆盘对最短穿透距离：max(0, |c_i−c_j| −
   (20−r_i) − (20−r_j))。注意与 A3 猜点窗口的衔接。
   量级估 20~80s/局。

B. 测点-清除耦合（TSPN 的对偶）：refine 点选择目标从"压 r"
   升级为"压 r 的同时最大化未来清除邻域 20−r 与顺路程度"
  （压 r 收益递减，20−r 是线性收益）——测量与清除的深层耦合。

C. 扫描覆盖巡回重构（最大胆）：先核实规则语义——测量必须在
   离散停点还是可在路径任意位置。若连续：扫描从"8 离散停点
   覆盖"升级为"最短覆盖巡回"，ring8 环游 ~6030m 未必最短；
   先算连续覆盖巡回理论下界 vs ring8 的差距，差距大才重构。
   若离散：也允许重新设计停点集合（不必是 ring），只要覆盖
   下界论证成立。

D. 任务统一形式化（Option R 终极形态）：absent 认证（覆盖楔形
   覆盖）、定位（测点/交会）、清除（TSPN 邻域）统一为"一组带
   异质邻域的访问需求 + 单条最短巡回"——三类任务同一数学结构，
   单一滚动规划器求解，三阶段作为涌现行为而非硬编码。滚动前瞻
  （每 k 动作/每清 2 源重解）已验证有效，继续深化。

E. 谱加权 FIM：目标函数对 FIM 特征值谱加权，抑制退化构型
  （交会角→0°/180°）。一行改动级，预实验验证收益上限（它只
   作用于 FIM 粗筛路径）。

F. 首方位偏置扫描序：第 1 停点方位信息重排剩余停点访问顺序
  （信号密集扇区优先），不破坏覆盖完整性。

G. 占用频道跳测 / 微停靠 / 双职责航向：已发现频道不再为 absent
   重测；行进中任意位置 +5s 停测（若规则允许）；航向同时优化
   absent 覆盖楔形与交会角（先核实 ±90° FOV 语义）。

H. 迷你 strip 入停止决策：区域 MEC∈(40,80] 时条带 vs 专程
   refine 的确定性成本比较接入决策点。

I. 贴边源特化：R=1000 下界 + 源贴 1800 边缘是最坏场景，专案
   设计测点/清除策略（别人容易漏的角落，极端压测必查）。

J. 两腿即证书：±1° 有界误差、交会角 90°、距离 ≤1140m 时两方位
   达 19.9m 证书——检查 ring 几何是否天然提供，若真 locate 段
   提前蒸发；配合"逐源 3+ 腿"清零核查。

K. BO 调参：对全部关键阈值（guess_clear、折算系数、前瞻深度、
   strip 阈值、扫描序偏置强度等）做贝叶斯优化，替代网格。

L. RL moonshot（后台，不占主力注意力）：simlite 即 gym
  （毫秒级一局，奖励=−剩余虚拟时间），变体任选（对偶控制/
   好奇心驱动/常规），架构必须=RL 提议+确定性校验器裁决。
   评估阶梯：simlite ≥500 局 vs 当时最优确定性版本，胜出才
   保留，任何失败归档不阻塞主线。训练日志必须能回答"学到了
   什么人类策略没有的东西"。

━━━ 工作循环 ━━━
每方向：理论/几何论证（写进文档）→ 预实验（小样本快筛）→
同池门1 对比 → 正收益并入组合（逐项翻，保证可归因）→ 决策表
登记（收益、证据路径、一行回滚）。随时可按预实验结果重排优先
级、放弃死路、发明新方向——你是负责人。

━━━ 收尾报告 docs/实验轮3_报告.md ━━━
1. 你的地板表（含推导，与我的数字谁对谁错）；
2. 最终结果按 N 档的"实测 vs 地板"对照表；
3. 架构变体演进链：每个改动收割了多少秒（从 450 起算）；
4. 失败方向归因：试了什么、为什么没用（完整保留，论文金子）；
5. 最终配置全景 + 每项的一行回滚命令；
6. 开放问题：如果再给一轮，你会攻哪里、预期还能压多少。

现在从任务0（地板核验）开始，然后按你的判断全力开干。

【追加·重要机制澄清：定格动画语义】
官方机制：虚拟时间只在动作执行时推进——两次指令之间的任何现实
计算（规划/求解/搜索）虚拟时间完全冻结，像定格动画。总窗口约束
是真实墙钟 25 分钟/局。

由此更新三条执行规则：
1. 计算量解禁：任何"更多真实计算换更优决策"的手段虚拟时间免费
  ——每动作全量重解 TSPN、加深前瞻、精确覆盖巡回求解、更密
   候选评估，都不再有虚拟时间成本。此前一切"在线计算成本折算"
  （包括为重规划设的间隔 k、决策点里给现实耗时预留的惩罚系数）
   一律重新评估：现实计算在虚拟时间上免费，只比较虚拟时间本身；
2. 唯一硬约束=墙钟 25 分钟：给现实耗时加监控——单次决策预算
   上限（建议 ≤2s/步，可按需调整）、单局总计算 ≤5 分钟量级、
   每局报告"现实总耗时 vs 25 分钟"的裕量。任何新模块必须报告
   其现实耗时增量；simlite 中同时记录虚拟与现实两个时钟；
3. 设计推论：决策质量的边际收益全部免费兑现——预实验对比时
  "算得多"的臂不再有现实成本劣势，尽情比；但墙钟监控数据
   必须同池报告，防止某个模块在极端情形（N=16、贴边）下把
   墙钟吃爆——毁掉一整局的代价远大于慢 100s。
```

## 运行结果

见 `docs/实验轮3_报告.md`（任务0 地板表、N 档对账、演进链、失败归因、
最终配置与回滚命令、开放问题）与 `ai_log/夜间自主优化_进度.md`（逐项
原子记录）。执行摘要：

- **任务0**：独立地板表推导完成（扫描 1970-2173 + BHH 环游 1438-1816 +
  clear 动作 + 残差定位顺路腿；N=10/13/16 ≈ 396/340/304 s/源；与用户
  数据一致，N≥13 档差 ~6% 为残差项口径）。
- **P1**：两腿证书率 28.9%；FIM 第 1 腿移动中位 1127m（locate 段主坑）。
- **P2/P4b**：tour_refine 腿选择器 4 版迭代 + 同池裁决 = 37/40 负 →
  **归档失败方向**（教训：保证接收域投影/横向交会/真相口径预测三要素；
  FIM 不预测胜于预测）。
- **P3**：交织扫描（interleaved_scan_refine）300 局 292/300 优、中位
  -1694s（-22.6%）、clear 失败 53→0 → **翻默认**。
- **P4**：顶配（含 tour）60/60 优 -44.8%（门1 全过）；P4b 把 tour 剔除。
- **A/TSPN**：清除端点数学保证 |p−源|≤20，翻默认。
- **P6 最终组合**（interleaved+TSPN vs 晨线）：120/120 全优、中位
  **-5305.9s（-48.9%）**、门1 六条全过、最坏恶化 0.0、N 混合 5693s 中位
  （≈438s/源 simlite；官方口径预计 395-420）。
- N 分档：N=16 档 387.9s/源（地板 304，+28%）；N=10 档 530（地板 396，
  **350 目标数学不可达**——证明入报告）；剩余差距归因完成（残腿交会
  几何下限 60-70% 不可收割，30% 过度移动列为开放问题）。
- 全量回归 147/147 通过；禁改最小集未触碰；无 Git 写操作（仅当前分支
  内文件改动，未 commit）。