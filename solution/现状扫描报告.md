# solution 现状扫描报告

扫描时间：2026-09-11  
执行阶段：操作手册第一步，仅扫描与诊断；未移动文件、未修改算法、未执行 Git。

## 1. 扫描范围与总体结论

递归扫描到 37 个 Python 文件、1 个 JavaScript 文件、1 个 HTML 文件和 1 个 CSS 文件。`output/` 下的 JSON/PNG/SVG 是生成产物，不作为源文件逐项列入。源码中的函数绝大多数没有参数类型和返回类型注解，以下签名按源码原样记录；数据类另外列出其声明字段。

当前目录已经接近手册目标结构，但尚未完成：

- Q1—Q4 和 `common` 已在 `solution/src/` 下；
- 自动化测试仍平铺在 `solution/tests/`，Q1 对拍与数据生成器仍在 `src/q1/testbench/`；
- 共享运行层位于 `solution/runtime/`，不在 `src/` 内，而且根 `.gitignore` 的 `runtime/` 规则会忽略它；
- README、PROJECT_CONTEXT 和部分测试仍使用迁移前的 `problem_b/code/` 路径；
- 不写缓存的诊断运行结果为 4 个失败、5 个导入错误：测试未把 `solution/src` 加入模块路径，Q1 CLI 测试还查找不存在的仓库根 `code/q1/cli.py`；
- `solution/src/q1/cli.py` 和 `solution/src/q2/cli.py` 可独立运行；`solution/src/q3/cli.py` 因找不到位于 `solution/runtime/` 的包而失败。

以上仅是当前可运行性诊断，不构成人工验证。依照操作手册，Q1—Q4 当前状态均应保持【待验证】。

## 2. Q1 源文件与接口

| 文件 | 函数/类签名 | 从代码归纳的作用 |
|---|---|---|
| `src/q1/__init__.py` | 无新定义；导出 `analyze_q1`、`localize` | Q1 公共导入入口。 |
| `src/q1/bearing_plot.py` | `_finite(value, name)` | 校验一个绘图数值为有限数。 |
| 同上 | `_point(point, name)` | 校验并标准化二维点。 |
| 同上 | `validate_inputs(detector_points, bearings_deg, source=None)` | 校验检测点、示向度和可选真值标记，不推断物理状态。 |
| 同上 | `plot_bearings(detector_points, bearings_deg, source=None, *, region=None, error_deg=ERROR_DEG, show=True, output_path=None)` | 绘制检测点、示向扇区、定位区域、直径和覆盖圆，可输出 PNG/SVG。 |
| `src/q1/cli.py` | `prompt_scene()`；内部 `ask(prompt)` | 交互读取测点、示向度和可选参考源。 |
| 同上 | `_display_bounds(points)` | 根据测点尺度生成仅供显示的画幅。 |
| 同上 | `analyze_q1(points, bearings, error_deg=1.0)` | 只用测点和示向度计算区域及逐次收缩记录，返回可 JSON 化字典。 |
| 同上 | `_print_result(analysis)` | 将 Q1 区域、直径和覆盖结论输出到控制台。 |
| 同上 | `main(argv=None)` | 解析 Q1 命令行参数并驱动分析、绘图和 JSON 导出。 |
| `src/q1/geometry.py` | `bearing_planes(position, bearing_deg, error_deg=1.0)` | 把一个正向示向扇区转成两个标准化半平面。 |
| 同上 | `contains(planes, point)` | 判断点是否满足全部半平面约束。 |
| 同上 | `hull(points)`；内部 `cross(o, a, b)`、`half(seq)` | 去重后用单调链求严格逆时针凸包。 |
| 同上 | `_distance_squared(p, q)` | 计算两点平方距离。 |
| 同上 | `diameter_bruteforce(vertices)` | 用 O(n²) 全点对计算凸多边形直径，作为独立基准。 |
| 同上 | `diameter_calipers(vertices)`；内部 `area2(edge_index, point_index)` | 对凸包使用旋转卡壳计算直径。 |
| 同上 | `diameter(vertices)` | 调用旋转卡壳的正式直径入口。 |
| 同上 | `minimum_enclosing_circle(vertices)` | 枚举一至三个支撑点，求小规模顶点集最小包围圆。 |
| 同上 | `intersect_halfplanes(planes)`；内部 `_empty()` | 枚举边界交点，分类空、无界、退化或有界区域并计算几何量。 |
| 同上 | `clip_to_box(planes, bounds)` | 只为绘图把半平面交裁剪到矩形画幅。 |
| 同上 | `localize(observations, bounds, error_deg=1.0)` | 读取字典观测中的 `status/position/bearing_deg`，构造纯示向定位结果。 |
| `src/q1/testbench/gen_cases.py` | `sample_error(rng, mode)` | 按 uniform/endpoints/mixed 三档产生角误差。 |
| 同上 | `_place_detectors(rng, source, reception, count)` | 在满足接收与目标域约束的条件下生成检测点。 |
| 同上 | `generate_case(seed, detector_count, error_mode)` | 生成一个带真值审计字段的确定性 Q1 案例。 |
| 同上 | `build_cases(base_seed, count_per_config)` | 穷举检测点数量和误差档组合生成案例集合。 |
| 同上 | `main(argv=None)` | 把案例和 manifest 写入指定目录。 |
| `src/q1/testbench/duipai.py` | `_wrap(delta)` | 把角差规约到以 0 为中心的周期区间。 |
| 同上 | `_observations(case)` | 从案例构造 Q1 求解器可见的观测字典。 |
| 同上 | `_check_data(case)` | 检查生成案例的字段、几何范围和测角误差。 |
| 同上 | `_check_region(case, region)` | 用真值、全点对和直接顶点计算审计区域、直径及圆覆盖。 |
| 同上 | `_check_contraction(case)` | 检查逐次加入相容观测后有界区域直径不增。 |
| 同上 | `check_case(case)` | 汇总单个案例的全部独立检查。 |
| 同上 | `main(argv=None)` | 读取 manifest，批量对拍并输出失败明细。 |

## 3. Q2 源文件与接口

| 文件 | 函数/类签名 | 从代码归纳的作用 |
|---|---|---|
| `src/q2/__init__.py` | 无新定义；导出 `Q2Config`、`build_candidate_regions`、`plan_measurement`、`plan_second_point` | Q2 公共导入入口。 |
| `src/q2/candidates.py` | `_clip_halfplane(polygon, plane, tolerance=1e-9)` | 用一个半平面裁剪凸多边形。 |
| 同上 | `_clip_many(polygon, planes)` | 连续应用多组半平面并重建凸包。 |
| 同上 | `_inner_circle_polygon(center, radius, sides)` | 构造圆的内接正多边形顶点。 |
| 同上 | `_inner_circle_planes(center, radius, sides)` | 生成内接正多边形的半平面表示。 |
| 同上 | `_summary(vertices, *, kind, relation, radius, sides, definition)` | 计算候选域面积、状态和近似元数据。 |
| 同上 | `build_candidate_regions(source_region, *, min_receive_radius=1000.0, max_receive_radius=1500.0, circle_sides=72)` | 输出保证接收域的内近似和可能接收域的外近似。 |
| 同上 | `generate_candidates(region, current_position, first_bearing_deg, *, guaranteed_region=None, forward_steps=(100.0,250.0,500.0,750.0), center_radii=(250.0,500.0,750.0), center_angles=8)` | 从前进/侧移、区域中心环和保证域中心生成确定性离散候选。 |
| `src/q2/planner.py` | `class Q2Config`：`error_deg: float=1.005`、`arena_radius: float=1800.0`、`min_receive_radius: float=1000.0`、`max_receive_radius: float=1500.0`、`circle_sides: int=24`、`candidate_region_sides: int=72`、`scenario_limit: int=8`、`uncertainty_seconds_per_metre: float=0.5` | 保存 Q2 物理边界、离散精度、场景数和评分权重。 |
| 同上 | `_posterior_radius(region, sensor, target, error, config)` | 对一个假想真值和测角误差模拟第二次观测后的包围圆半径。 |
| 同上 | `_fim_proxy(sensor, current_position, nominal_target, action_seconds)` | 计算交会角、距离和动作时间构成的 FIM 排序代理量。 |
| 同上 | `score_candidates(region, observations, candidates, current_position, current_channel, target_channel, config=Q2Config())` | 在有限源位置和误差场景上计算候选的最坏后验半径、动作时间和总分。 |
| 同上 | `plan_measurement(region, observations, *, current_position=None, current_channel=None, target_channel=None, config=Q2Config())` | 对已有有界区域生成并评分候选，优先返回保证接收候选中的最低分点。 |
| 同上 | `plan_second_point(first_observation, *, current_channel=None, config=Q2Config())` | 从一次 `BearingObservation` 构造物理区域并规划第二测点。 |
| `src/q2/plot.py` | `plot_plan(plan, *, output=None, show=True)` | 绘制源位置域、连续候选域、离散候选、选点和有限场景柱状图。 |
| `src/q2/cli.py` | `_jsonable(value)` | 递归把元组等对象转换为 JSON 友好结构。 |
| 同上 | `main(argv=None)` | 读取首次观测，执行 Q2 规划并输出控制台、JSON 和图形。 |

## 4. Q3/Q4 源文件与接口

| 文件 | 函数/类签名 | 从代码归纳的作用 |
|---|---|---|
| `src/q3/__init__.py` | 无新定义；导出 `Q3Policy` | Q3 公共入口。 |
| `src/q3/coverage.py` | `ring7()` | 返回中心加六边形环的 7 个全向覆盖测点。 |
| 同上 | `strip_clear_points(sensor, bearing_deg)` | 沿首次测向窄条带生成三行蛇形清除点。 |
| 同上 | `nearest_coverage_distance(point, centers)` | 计算目标点到给定覆盖点集的最近距离。 |
| `src/q3/policy.py` | `class SourceTrack`：频道、观测、区域、复测和保底进度字段 | 保存单频道干扰源的定位与清除状态。 |
| 同上 | `class Q3State`：进入/退出、位置、频道、时间、扫描阶段、源表等字段 | 保存整个 Q3 有限状态机状态。 |
| 同上 | `class Q3Policy`；`__init__(self, *, max_refinements=2, error_deg=1.005, coverage_points=None)` | 配置全向扫描点、复测次数和 Q2 子规划器。 |
| 同上 | `initial_state(self)` | 创建初始 Q3State。 |
| 同上 | `_action(self, state, kind, position=None, channel=None, mode=None)` | 创建带顺序 request_id 的待处理动作。 |
| 同上 | `_scan_action(self, state)` | 按覆盖点和频道生成下一扫描动作并形成无源集合。 |
| 同上 | `_refinement_point(self, state, track)` | 调用 Q2 `plan_measurement` 选择复测点。 |
| 同上 | `_resolve_action(self, state)` | 在认证清除、Q2 复测和蛇形保底清除间选择下一动作。 |
| 同上 | `next_action(self, state)` | 按当前阶段返回 enter/measure/clear/exit 动作。 |
| 同上 | `_record_measurement(self, state, action, response)` | 解析 near/no_signal/direction 并更新频道定位区域。 |
| 同上 | `apply_response(self, state, action, response)` | 校验响应并推进位置、频道、复测、清除和退出状态。 |
| `src/q3/cli.py` | `main(argv=None)` | 使用本地 FakeSimulator 演示 Q3 并导出动作摘要。 |
| `src/q4/__init__.py` | 无新定义；导出 `Q4Policy` | Q4 公共入口。 |
| `src/q4/directional.py` | `grid121()` | 生成 11×11 蛇形定向源覆盖网格。 |
| 同上 | `four_sided_points(center, radius, rho=200.0)` | 满足半径证书时生成定位圆四侧复核点，否则返回空。 |
| 同上 | `is_visible(source, direction_deg, sensor, receive_radius=1000.0)` | 判断检测点是否同时满足定向半平面和接收距离。 |
| `src/q4/policy.py` | `class Q4Policy(Q3Policy)`；`__init__(self, *, max_refinements=2, error_deg=1.005)` | 用 121 点网格初始化继承自 Q3 的定向策略。 |
| 同上 | `_refinement_point(self, state, track)` | 优先使用四侧复核点，证书不适用时退回 Q3/Q2 复测。 |
| `src/q4/cli.py` | `main(argv=None)` | 使用本地定向 FakeSource 演示 Q4 并导出动作摘要。 |

## 5. 多题公用逻辑与运行层

| 文件 | 函数/类签名 | 从代码归纳的作用 |
|---|---|---|
| `src/common/__init__.py` | 无定义 | 当前仅标记 common 包。 |
| `src/common/domain.py` | `circle_outer_planes(center, radius, sides=32)` | 用外切正多边形半平面保守外包圆形区域。 |
| 同上 | `build_region_from_observations(observations, *, error_deg=1.005, arena_radius=1800.0, max_receive_radius=1500.0, circle_sides=32)` | 合并目标圆、接收上界圆和有效示向，构造 Q2—Q4 物理定位区域。 |
| 同上 | `representative_points(vertices, limit=12)` | 从边界顶点、中点和质心确定性抽取有限评分场景。 |
| 同上 | `max_vertex_distance(point, vertices)` | 求一个点到区域顶点的最大距离。 |
| `src/common/models.py` | `class BearingObservation(position: Point, channel: int, result: MeasureKind, bearing_deg: float|None=None)`；`__post_init__(self)`；`as_dict(self)` | 表示并校验一次频道测向观测，支持字典导出。 |
| 同上 | `class Action(kind: ActionKind, request_id: str, position: Point|None=None, channel: int|None=None)`；`as_dict(self)` | 表示策略动作并按位置对象格式导出。 |
| 同上 | `class TimeBreakdown(movement_s=0, switching_s=0, measurement_s=0, optical_s=0, laser_s=0)`；属性 `total_s(self)`；`as_dict(self)` | 保存虚拟时间分项并计算总时间。 |
| 同上 | `class RunSummary(actions=[], cleared_channels=[], absent_channels=[], virtual_time_s=0, exit_reason=None)`；`as_dict(self)` | 保存一次策略执行摘要。 |
| `src/common/time_model.py` | `measure_cost(previous, target, current_channel, target_channel)` | 按移动速度、切台和检测耗时计算 measure 时间。 |
| 同上 | `clear_cost(previous, target, success)` | 按移动、光学确认及成功激光耗时计算 clear 时间。 |
| `runtime/__init__.py` | 无定义 | 标记模拟器无关运行层包。 |
| `runtime/protocol.py` | `class RobotClient(Protocol)`；`execute(self, action: Action) -> dict` | 定义策略执行器所需的最小客户端协议。 |
| `runtime/runner.py` | `run_policy(policy, client, *, max_actions=10000)` | 串行调用策略与客户端，直到退出或动作数超限。 |
| `runtime/fake_simulator.py` | `class FakeSource(channel: int, position: tuple[float,float], receive_radius: float=1000.0, direction_deg: float|None=None, cleared: bool=False)` | 表示本地规则模型中的全向或定向源。 |
| 同上 | `class FakeSimulator`；`__init__(self, sources, error_bound_deg=1.0)` | 初始化本地源、机器人状态、虚拟时间和幂等缓存。 |
| 同上 | `_visible(self, source, sensor)` | 判断源是否在接收半径和定向可见半平面内。 |
| 同上 | `_error(self, source, sensor)` | 由频道和位置哈希生成同地固定测角误差。 |
| 同上 | `execute(self, action: Action)` | 模拟 enter/measure/clear/exit，更新时间并缓存同 request_id 响应。 |
| `runtime/http_client.py` | `build_request_payload(action, robot_id)` | 把内部 Action 转换为当前假定的 HTTP 请求字段。 |
| 同上 | `class HttpRobotClient`；`__init__(self, robot_id, base_url='http://127.0.0.1:2026', timeout_s=5.0, retry_count=2, retry_delay_s=0.2)` | 保存同步 HTTP 客户端配置和串行锁。 |
| 同上 | `execute(self, action)` | 向 `/<action.kind>` POST JSON，并以相同请求体和 ID 重试传输错误。 |

## 6. 已归入 legacy 的旧实验链

| 文件 | 函数/类签名 | 从代码归纳的作用 |
|---|---|---|
| `src/legacy/environment.py` | `number(value,name,low=-2000000,high=2000000)`；`integer(value,name,low,high)` | 校验合成场景中的数值和整数。 |
| 同上 | `validate_scene(data)` | 校验合成源、测点、频道和误差配置。 |
| 同上 | `measure(source, detector, seed)` | 用真值和固定哈希误差生成 no_signal/near/direction。 |
| 同上 | `calculate(data)` | 对合成场景产生观测、调用 Q1 并附加真值审计。 |
| 同上 | `generate(data)`；内部 `disk(radius, center=(0,0))` | 按随机种子生成合成源和检测点。 |
| 同上 | `demo()` | 返回固定浏览器演示场景。 |
| `src/legacy/server.py` | `class Handler(BaseHTTPRequestHandler)`；`send(self,status,body,content_type='application/json')`；`do_GET(self)`；`do_POST(self)` | 提供静态页面以及 demo/calculate/generate 本地 HTTP 接口。 |
| 同上 | `main()` | 在本机 127.0.0.1 启动实验服务器。 |
| `src/legacy/visualization/app.js` | `$(id)`、`fmt(v,digits=2)`、`message(text,error=false)`、`markDirty()` | DOM 查询、数值格式化和页面状态提示。 |
| 同上 | `numericInput(value,label,min,max)`、`appendCell(row,element)`、`sourceRow(s,index)`、`detectorRow(d,index)`、`renumberDetectors()` | 构造并维护源和检测点输入表。 |
| 同上 | `syncChannels(preferred=...)`、`populate(scene)`、`value(input,optional=false)`、`readScene()` | 同步频道、装载场景并读取/校验表单。 |
| 同上 | `element(tag,attrs={},text=null)`、`draw(svg,bounds,local=false)`、`render()` | 构造 SVG 并绘制全局/局部定位结果。 |
| 同上 | `download(text,name,type)`、`exportSVG(id,name)` | 下载 JSON/SVG 等浏览器产物。 |
| `src/legacy/visualization/index.html` | 无函数/类 | 旧实验界面的 DOM、输入表和 SVG 容器。 |
| `src/legacy/visualization/style.css` | 无函数/类 | 旧实验界面的布局、颜色和响应式样式。 |

静态引用结果：`environment.py` 仍被 `tests/test_geometry.py` 使用；`server.py` 及前端资源只组成旧浏览器链，不在 Q1—Q4 正式入口中。它们已位于 legacy，但尚缺手册要求的逐文件 README。

## 7. 现有测试源文件

| 文件 | 函数/类签名 | 一句话检查目标 |
|---|---|---|
| `tests/__init__.py` | 无定义 | 标记测试包。 |
| `tests/test_geometry.py` | `class GeometryTests(unittest.TestCase)` | Q1 几何和区域不变量测试集合。 |
| 同上 | `test_rectangle_analytic_diameter(self)` | 手算 3×4 矩形面积、直径和直径圆。 |
| 同上 | `test_triangle_diameter_circle_does_not_cover(self)` | 检查等边三角形直径圆反例。 |
| 同上 | `test_longest_pair_can_be_edge(self)` | 检查最长点对为边的凸包。 |
| 同上 | `test_empty(self)`、`test_single_point(self)`、`test_line_segment(self)`、`test_parallel_strip_is_unbounded(self)` | 检查空、点、线段和无界退化状态。 |
| 同上 | `test_unbounded_region_never_uses_viewport_diameter(self)` | 确认显示画幅不伪造无界区域直径。 |
| 同上 | `test_wraparound_and_forward_ray(self)` | 检查跨 0° 和正向射线。 |
| 同上 | `test_bounded_result_independent_of_viewport(self)`、`test_translation_invariance(self)` | 检查画幅独立性和平移不变量。 |
| 同上 | `test_synthetic_truth_and_monotonic_contraction(self)` | 用 60 个合成种子检查真值包含与逐次收缩。 |
| 同上 | `class EnvironmentTests(unittest.TestCase)` | 旧合成环境行为测试集合。 |
| 同上 | `test_cardinal_bearings(self)`、`test_fixed_error_same_location_and_channel(self)`、`test_repeat_observation_does_not_shrink(self)` | 检查基准方向、固定误差和同地重复观测。 |
| 同上 | `test_conflicting_same_place_errors_rejected(self)`、`test_valid_direction_input_contract(self)`、`test_source_channel_and_finite_validation(self)` | 检查合成输入拒绝规则。 |
| 同上 | `test_error_bound_endpoints(self)`、`test_deterministic_generation_and_domain(self)`、`test_outside_domain_detector_can_still_be_valid(self)`、`test_solver_does_not_require_truth(self)` | 检查误差端点、确定生成、域外测点和真值隔离。 |
| `tests/test_q1_algorithms.py` | `class Q1AlgorithmTestbench(unittest.TestCase)` | Q1 主算法与数学结论测试集合。 |
| 同上 | `test_rotating_calipers_matches_all_pairs_on_random_hulls(self)` | 2000 个随机凸包对拍旋转卡壳和全点对。 |
| 同上 | `test_rotating_calipers_handles_degenerate_polygons(self)` | 检查空、单点和共线输入。 |
| 同上 | `test_minimum_enclosing_circle_triangle_counterexample(self)`、`test_two_bearing_wedges_can_reject_diameter_circle(self)` | 检查解析和测向形式的直径圆反例。 |
| 同上 | `test_error_margin_expands_or_preserves_demo_region(self)` | 检查 1.005° 区域不小于 1° 区域。 |
| 同上 | `test_progress_uses_only_observations_and_contracts(self)` | 检查逐次收缩且输入不含源真值。 |
| 同上 | `test_invalid_error_bound_is_rejected(self)` | 检查非法误差界。 |
| `tests/test_q1_cli.py` | `class Q1CliTestbench(unittest.TestCase)`；`run_cli(self,*arguments)` | 封装 Q1 CLI 子进程调用。 |
| 同上 | `test_demo_exports_png_and_json(self)`、`test_direct_measurements_export_svg(self)` | 检查演示和直接输入的 JSON/PNG/SVG。 |
| 同上 | `test_single_measurement_reports_unbounded_region(self)`、`test_invalid_bearing_fails_without_artifact(self)` | 检查无界分支和非法输入失败行为。 |
| `tests/test_q2.py` | `class Q2AlgorithmTestbench(unittest.TestCase)`；`setUp(self)` | 构造固定源、首次测点和相容 BearingObservation。 |
| 同上 | `test_physical_region_is_bounded_and_contains_truth(self)`、`test_plan_is_deterministic_and_selected_from_candidates(self)` | 检查物理区域和确定性选点。 |
| 同上 | `test_selected_is_best_guaranteed_candidate_when_available(self)`、`test_no_guaranteed_candidate_falls_back_without_crash(self)` | 检查保证候选优先及空保证域回退。 |
| 同上 | `test_continuous_candidate_regions_have_auditable_bounds(self)` | 检查连续内/外近似方向和顶点距离。 |
| 同上 | `test_non_direction_input_is_rejected(self)`、`test_all_scores_are_finite_and_have_time_breakdown(self)` | 检查输入拒绝、有限分数和时间分解。 |
| 同上 | `test_selected_measurement_keeps_compatible_truth_and_contracts(self)` | 检查一组相容第二观测不排除真值且半径不增。 |
| 同上 | `class Q2CliTestbench(unittest.TestCase)`；`test_cli_writes_json_and_png(self)` | 检查 Q2 CLI JSON、PNG 和关键字段。 |
| 同上 | `_inside_convex(polygon, point, tolerance=1e-6)` | 测试侧的凸多边形点包含判定。 |
| `tests/test_q3_offline.py` | `class Q3TheoryTestbench(unittest.TestCase)` | Q3 现有离线理论和状态机测试集合。 |
| 同上 | `test_ring7_covers_random_target_disk_at_radius_1000(self)`、`test_strip_clear_grid_covers_full_bearing_rectangle(self)` | 随机检查 7 点覆盖和蛇形清除覆盖。 |
| 同上 | `test_official_199_second_accounting_example(self)` | 按代码时间模型复算一个 199 秒动作序列。 |
| 同上 | `test_request_id_is_idempotent_in_fake_client(self)`、`test_official_payload_does_not_leak_internal_fields(self)` | 检查本地幂等和 HTTP 负载字段。 |
| 同上 | `test_q3_policy_finitely_clears_and_certifies_channels_offline(self)` | 检查单个全向源在动作上限内结束。 |
| `tests/test_q4_offline.py` | `class Q4TheoryTestbench(unittest.TestCase)` | Q4 现有离线方向覆盖和状态机测试集合。 |
| 同上 | `test_grid121_covers_random_positions_and_directions(self)` | 随机检查 121 点网格的定向可见性。 |
| 同上 | `test_four_sided_certificate_and_rejection_boundary(self)`、`test_four_sided_points_have_a_visible_member(self)` | 检查四侧复核的适用条件和可见成员。 |
| 同上 | `test_q4_policy_finitely_clears_directional_source_offline(self)` | 检查一个定向源在动作上限内结束。 |

## 8. 初步归类

### Q1 实现

- `src/q1/__init__.py`、`bearing_plot.py`、`cli.py`、`geometry.py`。
- `src/q1/testbench/` 实际属于 Q1 测试工具，建议第二步移入 `tests/q1/`，不保留在生产源码目录。
- 现有 `tests/test_geometry.py` 的 GeometryTests、`tests/test_q1_algorithms.py`、`tests/test_q1_cli.py` 属于 Q1 测试。

### Q2 实现

- `src/q2/__init__.py`、`candidates.py`、`planner.py`、`plot.py`、`cli.py`。
- `tests/test_q2.py` 属于 Q2 测试。

### Q3 实现

- `src/q3/__init__.py`、`coverage.py`、`policy.py`、`cli.py`。
- `tests/test_q3_offline.py` 是既有 Q3 离线测试，但手册要求本轮 Q3/Q4 不建测试，如何处置需人工确认。

### Q4 实现

- `src/q4/__init__.py`、`directional.py`、`policy.py`、`cli.py`。
- `tests/test_q4_offline.py` 是既有 Q4 离线测试，处置同样需人工确认。

### 多题复用

- `src/common/models.py`、`domain.py`、`time_model.py` 被 Q2—Q4 复用；其中 domain 进一步依赖 Q1 几何。
- `runtime/protocol.py`、`runner.py`、`fake_simulator.py`、`http_client.py` 被 Q3/Q4 CLI 或测试复用。
- `q3.policy.Q3Policy` 被 Q4 继承；Q3 复测调用 Q2，Q2/common 调用 Q1。

### 疑似废弃或旧实验文件

- `src/legacy/environment.py`、`server.py`、`visualization/*` 已明显标注为本地旧合成实验链。
- `environment.py` 仍被回归测试引用，因此是“非正式运行、仍有测试价值”，不宜在未确认前删除。
- `server.py` 与前端资源没有被 Q1—Q4 正式入口引用，可继续归档在 legacy；需补每文件 README。

### 归类不明确

- `solution/runtime/`：是 Q3/Q4 共享运行层，但手册目标树未规定 `src/runtime/`；当前还被 `.gitignore` 忽略。
- `tests/test_geometry.py` 的 EnvironmentTests：依赖 legacy 合成环境，放 `tests/q1/`、`tests/legacy/` 或停止保留需人工决定。
- 已有 `tests/test_q3_offline.py` 与 `tests/test_q4_offline.py`：手册说“不建测试”，但删除既有测试会损失回归能力。
- 根级历史验收文件 `test_res_q1.md` 等：与手册要求的新 `tests/qN/test_res.md` 人工模板职责不同，需决定保留、改名归档或引用。

## 9. 无法确定的接口语义与需人工确认事项

1. **runtime 的正式位置**：建议在目标树中新增 `solution/src/runtime/`，保留为 Q3/Q4 共享包，并调整 `.gitignore` 使其可被版本管理；是否同意？若必须严格使用手册现有树，备选是 `src/common/runtime/`。
2. **Q3/Q4 既有测试如何处理**：建议保留并分别归位到 `tests/q3/`、`tests/q4/`，但不在本轮新增对拍器；这是否符合“只做代码归位、不建测试”的本意？
3. **Q2 对拍的“唯一正确答案”指什么**：当前第二点选择包含人为评分权重 `0.5 s/m` 和有限场景近似，数学上不是唯一真实最优。建议 `test_duipai.py` 只独立核对候选域几何、时间记账和“给定候选/给定评分定义下的枚举最优”，不宣称对真实连续策略全局最优；是否认可？
4. **legacy 环境回归测试归属**：建议把 GeometryTests 并入 `tests/q1/test_unit.py`，把 EnvironmentTests 放 `tests/legacy/test_environment.py`；但目标树未列 `tests/legacy/`。是允许新增该目录，还是把两者都放 Q1？
5. **HTTP 模拟器协议代码**：操作手册明确协议未知，但 `runtime/http_client.py` 已硬编码 `arena_id/robot_id/request_id/position/channel` 和 `/<kind>`。建议在确认附件协议前保留文件、在 API 文档标“协议待补充”，不把这些字段写成已确认契约；是否同意？
6. **历史验收记录**：建议保留根级 `test_res_q1.md` 等作为历史自动测试证据，同时新建 `tests/q1/test_res.md`、`tests/q2/test_res.md` 空白人工模板，并在新模板中明确历史记录不等于本轮人工验证；是否同意？

## 10. 建议的第二步移动映射（待人工确认，尚未执行）

- `solution/runtime/` → `solution/src/runtime/`；
- `solution/src/q1/testbench/gen_cases.py` → `solution/tests/q1/gen_data.py`；
- `solution/src/q1/testbench/duipai.py` → `solution/tests/q1/test_duipai.py`；
- `solution/tests/test_q1_algorithms.py` 与 GeometryTests → `solution/tests/q1/test_unit.py`；
- `solution/tests/test_q1_cli.py` → `solution/tests/q1/test_cli.py`（辅助端到端测试）；
- `solution/tests/test_q2.py` 拆为 `tests/q2/test_unit.py`、`test_duipai.py`，另补 `gen_data.py`；
- Q3/Q4 源码保持在 `src/q3/`、`src/q4/`；既有离线测试是否归位待问题 2 确认；
- legacy 文件保持在 `src/legacy/`，只补逐文件 README，不删除现有代码。

根据操作手册，必须在人工确认上述归类与问题后，才能进入目录重组。
