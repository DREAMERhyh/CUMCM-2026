# Q4：含定向干扰源的定位与清除

题意：在 Q3 基础上处理具有发射方向、背面可能完全无信号的干扰源，需要设计更稳健的搜索、复核和清除策略。

当前状态：【待验证】。

策略未定、待对拍方案确定后补全。现有 `policy.py`、`directional.py` 和离线测试仅作为算法原型保留，不能替代官方模拟器验收。

Q4 继承 Q3 的共享源位置构造器，因此同样接收经过局部圆弧切线细化的保守凸多边形；这只提高位置区域近似精度，不改变发射方向可见性模型和四侧探测逻辑。

Q3 新增的“清除失败后条件复测”依赖全向源的保证接收判断。Q4 当前显式设置 `failed_clear_remeasure_mode="off"`，没有继承该行为；只有将未知发射方向纳入位置—方向联合可见性判断后才能另行启用。

Q3 新增的总虚拟时间滚动评价同样只证明了全向源的距离接收条件。Q4 显式设置 `rolling_time_mode="off"`，不会把 Q3 的有限位置场景误当成位置—发射方向联合场景。

`cli.py` 仍只运行本地 `sim.fake.FakeSimulator`。官方通信、三动作演练烟雾测试和现场日志入口位于 `src/sim/`；在人工完成 `tests/sim/verify_manual.md` 前不得把通信层本地通过理解为 Q4 策略通过。
