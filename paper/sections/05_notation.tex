\section{符号说明}

\begin{table}[H]
  \centering
  \caption{问题一符号说明}
  \label{tab:symbols}
  \begin{tabularx}{0.9\textwidth}{@{}cXc@{}}
    \toprule
    符号 & 含义 & 单位 \\
    \midrule
    $S_i=(x_i,y_i)$ & 第 $i$ 个检测点的二维位置坐标 & $\mathrm{m}$ \\
    $\theta_i$ & 第 $i$ 个检测点在位置 $S_i$ 处测得的示向度 & $^\circ$ \\
    $\delta$ & 示向度的最大误差，本文取 $1^\circ$ & $^\circ$ \\
    $\Omega$ & 所有角度误差区域的公共部分，即定位区域 & -- \\
    $V_j$ & 定位凸多边形的第 $j$ 个顶点 & m \\
    $D$ & 定位区域的直径 & m \\
    $V_p,V_q$ & 一组取得直径的端点 & m \\
    $O,r$ & 直径圆的圆心和半径 & m \\
    \bottomrule
  \end{tabularx}
\end{table}

\begin{table}[H]
  \centering
  \caption{问题二符号说明}
  \label{tab:q2-symbols}
  \begin{tabularx}{0.94\textwidth}{@{}cXc@{}}
    \toprule
    符号 & 含义 & 单位 \\
    \midrule
    $K_1$ & 第一次检测后同时考虑示向误差、目标圆形区域和接收距离上界的可行域 & -- \\
    $\widehat K_1$ & 以固定边数外切正多边形代替圆形区域后得到的 $K_1$ 保守外近似 & -- \\
    $S_2=s$ & 待选择的第二检测点位置坐标 & $\mathrm{m}$ \\
    $R_{\min},R_{\max}$ & 全向干扰源有效接收半径的下界与上界 & $\mathrm{m}$ \\
    $\mathcal F_g$ & 第二检测点的保证接收域 & -- \\
    $\mathcal F_p$ & 第二检测点的可能接收域 & -- \\
    $\rho(A)$ & 集合 $A$ 的定位不确定性半径，即最小包围圆半径 & $\mathrm{m}$ \\
    $R_{\mathrm{wc}}(s)$ & 二次定位不确定性半径的有限场景最坏预测值 & $\mathrm{m}$ \\
    $T(s)$ & 移动至 $s$ 并完成检测的行动时间 & $\mathrm{s}$ \\
    $J(s)$ & 有限离散候选点 $s$ 的综合评分值 & $\mathrm{s}$ \\
    $\Phi_{\mathrm{rob}}(s)$ & 对有限源位置场景取最小值的单位时间 Fisher 信息指标 & $\mathrm{m}^{-4}\mathrm{s}^{-1}$ \\
    $s_A,s_F,s^*$ & 离散方案代表点、连续方案代表点及最终推荐点 & $\mathrm{m}$ \\
    \bottomrule
  \end{tabularx}
\end{table}

\begin{table}[H]
  \centering
  \caption{问题三符号说明}
  \label{tab:q3-symbols}
  \begin{tabularx}{0.94\textwidth}{@{}cXc@{}}
    \toprule
    符号 & 含义 & 单位 \\
    \midrule
    $\mathcal D$ & 以 $O_0$ 为圆心、半径为 $1800\ \mathrm{m}$ 的目标圆形区域 & -- \\
    $P_k$ & 第 $k$ 个扫描驻留点，$k=0,1,\ldots,7$ & $\mathrm{m}$ \\
    $p$ & 机器狗的当前位置坐标 & $\mathrm{m}$ \\
    $v$ & 机器狗移动速度，题设取 $5$ & $\mathrm{m/s}$ \\
    $\tau_{\mathrm{sw}},\tau_{\mathrm{meas}}$ & 换频时间和单次检测时间 & $\mathrm{s}$ \\
    $\mathcal C_f,\mathcal C_a,\mathcal C_c$ & 已发现、判定无源和已经清除的频道集合 & -- \\
    $\widehat K_j^{(m)}$ & 频道 $j$ 获得 $m$ 次有效示向度后的源位置外近似 & -- \\
    $c_j,r_j$ & $\widehat K_j^{(m)}$ 的最小包围圆圆心和半径 & $\mathrm{m}$ \\
    $r_{\mathrm{loc}}$ & 完成可靠定位的半径阈值，程序取 $19.9$ & $\mathrm{m}$ \\
    $r_{\mathrm{try}}$ & 中心试清除阈值，程序取 $40$ & $\mathrm{m}$ \\
    $M$ & 每个源在扫描后允许的补充定位次数上限，程序取 $2$ & 次 \\
    $F(A,j)$ & 访问中心集合 $A$ 且以中心 $j$ 结束的最短内部路径长度 & $\mathrm{m}$ \\
    $Q_j$ & 频道 $j$ 的有序清除点队列 & -- \\
    \bottomrule
  \end{tabularx}
\end{table}

\begin{table}[H]
  \centering
  \caption{问题四符号说明}
  \label{tab:q4-symbols}
  \begin{tabularx}{0.94\textwidth}{@{}cXc@{}}
    \toprule
    符号 & 含义 & 单位 \\
    \midrule
    $\kappa_j$ & 频道 $j$ 的干扰源类型，取全向或定向 & -- \\
    $\psi_j$ & 定向干扰源最大场强方向的方位角 & $^\circ$ \\
    $V(\xi,s)$ & 可能状态 $\xi$ 在检测点 $s$ 是否能够接收的指示量 & -- \\
    $\mathcal P_4$ & 问题四全域扫描驻留点集合 & -- \\
    $\Xi_j$ & 频道 $j$ 的源位置、类型、接收半径和发射方向的有限可能状态集合 & -- \\
    $\mathcal G_j$ & 频道 $j$ 当前使用的多方向探测组 & -- \\
    $C_{\mathrm{base}}$ & 从当前检测点开始完成预计清除点列的基础时间 & $\mathrm{s}$ \\
    $C_{\mathrm{clr}}$ & 立即执行当前清除覆盖的预计剩余时间 & $\mathrm{s}$ \\
    $C(\pi,\xi)$ & 探测顺序 $\pi$ 在可能状态 $\xi$ 下的预计完成时间 & $\mathrm{s}$ \\
    $\alpha_R$ & 尾部风险评价的分位水平，本文取 $0.9$ & -- \\
    $\Delta_s$ & 继续检测相对立即清除所需的最小节省余量，本文取 $10$ & $\mathrm{s}$ \\
    \bottomrule
  \end{tabularx}
\end{table}
