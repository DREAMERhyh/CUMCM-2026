# AI 使用记录规则

所有在本仓库工作的 AI agent（包括主 agent、子 agent，以及仅阅读、分析或审查的 agent）都必须遵守本文件。记录用于最终整理竞赛要求的 `AI 工具使用详情.pdf`。

## 工作时如何记录

1. 开始一项任务时，在 `ai_log/` 中创建自己的记录，参照 [记录模板](ai_log/TEMPLATE.md)。文件名使用 `YYYYMMDD-HHMMSS_agent标识_任务简称.md`，必要时添加后缀避免重名。不要让多个 agent 同时编辑同一份记录。
2. 简要填写工具名称、版本或模型、任务目的，以及主要提示和使用过程。不能确认的型号或版本写“未知/待人工补充”，不得猜测；主要提示可摘要，不要求保存完整对话。
3. 在产生重要输出、改变方法或交接任务时补充记录，结束前更新结果和状态。简短任务可用一条记录覆盖全过程，不必逐条记录工具调用。
4. 写明输出文件或结论、采纳情况、修改情况，以及实际执行的检查和结果。尚不知道是否采纳时写“待决定”，没有检查时写“未核验”。失败或未采纳的尝试可以一句话概括。
5. **agent 自检不等于人工核验。** 人工审查栏默认写“待人工审查”，只有收到队员明确反馈后才记录其审查情况，并注明反馈依据；不得替队员声称已审查、已采纳或已核实。

## 协作与边界

- 每个 agent 负责自己的记录；主 agent 在交接时检查参与任务的 agent 是否留下记录，缺失时要求补充。若主 agent 代记，须注明代记及信息来源。
- 任务恢复时可继续自己的原记录；新的独立任务另建记录。保留实质性修改和核验过程，不为美化结果删除失败或未核验信息。
- 日志只写与任务有关的必要信息，不记录密钥、账号等敏感内容；人员使用“队员 A/B/C”等匿名标识，不填写姓名、学校、赛区。
- 核心建模与分析由参赛队主导，AI 参与完成的内容需由队员逐项审查与核实。

## 最终汇总

比赛提交前，由队员审核日志，再按 [汇总说明](ai_log/README.md) 整理规定的 PDF。日志是过程记录，不能直接视为已完成人工审核的提交材料。
