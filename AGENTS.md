# AI 使用记录规则

所有在本仓库工作的 AI agent（包括主 agent、子 agent，以及仅阅读、分析或审查的 agent）都必须遵守本文件。记录用于最终整理竞赛要求的 `AI 工具使用详情.pdf`。

## 工作时如何记录

1. 开始一项任务时，在 `ai_log/` 中创建自己的记录，参照 [记录模板](ai_log/TEMPLATE.md)。文件名使用 `YYYYMMDD-HHMMSS_agent标识_任务简称.md`，必要时添加后缀避免重名。不要让多个 agent 同时编辑同一份记录。
2. 每份记录只保留“输入”和“运行结果”两个部分。输入必须逐字记录完整的用户提示词；若任务由上级 agent 分派，则逐字记录完整的分派内容。不得用摘要、转述或省略号替代。
3. 运行结果简要写明实际完成的工作、输出文件或结论，以及执行的检查和结果。失败的尝试或未核验的内容如会影响结果，应在此如实说明。
4. 记录不得包含系统提示、开发者指令、工具内部参数、思维过程、密钥、账号或其他与用户任务无关或敏感的内容。

## 协作与边界

- 每个 agent 负责自己的记录；主 agent 在交接时检查参与任务的 agent 是否留下记录，缺失时要求补充。
- 任务恢复时可继续自己的原记录；新的独立任务另建记录。运行结果应保留实质性输出和核验信息，不为美化结果删除失败或未核验内容。
- 日志只写与任务有关的必要信息，不记录密钥、账号等敏感内容；人员使用“队员 A/B/C”等匿名标识，不填写姓名、学校、赛区。
- 核心建模与分析由参赛队主导，AI 参与完成的内容需由队员逐项审查与核实。

## 最终汇总

比赛提交前，由队员审核日志，再按 [汇总说明](ai_log/README.md) 整理规定的 PDF。

## B 题 solution/ 协作规范

- 目录约定：`solution/src/q1` 至 `q4` 放分题实现，`src/common` 放共享逻辑，`src/runtime` 放运行层，`src/legacy` 放仅为回归保留的旧代码；测试按题放在 `solution/tests/qN`，和模拟器相关通信相关的代码放在`src/sim`。
- 文档职责：分题 `README.md` 解释算法与代码对应关系；Q1/Q2 `API.md` 是测试数据和模块边界的接口契约；`verify_manual.md` 是人工状态判定的唯一清单；`test_res.md` 由执行验证的人工填写；`快速上手指南.md` 提供全局图和状态总表；`TODO.md` 只放未实施方案；`PROJECT_CONTEXT.md` 保存长期任务状态。
- 修改分题代码后，同步检查并更新该题 README 的算法对应表；若签名、字段、单位或返回语义变化，同步提出 API.md 变更并等待人工确认；全局状态表只能由人工根据验证结果更新。
- API.md 必须来自现有真实函数签名。任何接口变更须先列出影响并经人工确认，不得凭空设计官方协议。
- 状态标准：【未开始】无代码；【待验证】代码已写且本地可运行但未按人工清单验收；【已验证】人工按清单通过后标记；【模拟器未测试】本地已验证而官方模拟器尚未实测时由人工标记。AI 不得标记【已验证】或【模拟器未测试】。
- 禁止 AI 执行任何 Git 操作，包括 add、commit、push、pull、branch、mv、checkout、reset、tag 等。
- 遇到参数单位、返回值、调用时序、官方协议或状态语义不确定时，立即停止相关修改，整理问题并询问人工。
