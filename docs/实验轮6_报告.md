# 实验轮 6 报告（整晚自主探索）

> 目标：官方 405 → 330-360 s/源。官方基线 = 11 局均时 ~405（M9+tspn 生效）。
> 全部结论以 simlite 同池对拍为准；官方口径差异另列。

## 1. 规则核查包六项（完成）
| 项 | 结论 | 依据 |
|---|---|---|
| a) 已发现频道可豁免覆盖矩阵 | **成立** | 附件2 1.3：每频道至多一源（保证）→ 已发现频道不可能再有新源 |
| b) 每频道至多一源 | **保证** | 附件2 1.3 原文 |
| c) R 对狗隐藏 | **是** | 附件2：接口不返回有效接收半径；只给 [1000,1500] 界 |
| d) 单次驻留可测多频道 | **是** | B 题附录2(4)：变换频道 1s 后可连续测（当前已用） |
| e) 路径中途可测量 | **是** | "任何一点检测时先停止移动"→ 任意点可停测 |
| f) 切换成本 1s/任意两频道 | **确认** | 附件2 4.3（当前已按此计价） |

- a 的直接推论（猜1'）：已发现频道的重复测量无新增保证 → 按交会价值跳测。
- c 的推论（猜2）：R 在线辨识合法（观察 direction→no_signal 边界）。

## 2. 猜想结果（全部完成裁决）
### 猜1' 角度判据交织跳测（**晋级默认**）
- 规则前提 a/b 成立（每频道至多一源）；30 局门1 六条**全过**、中位 -85.6s
  （扫描段 -104s 30/30 全降）、tail 6768→6697；最坏恶化 447.3s（3 局之一）。
- 机制：张角 <30° 或 r<=15m 的已发现频道重复测量跳过（省 5s+切换，移动照走）。
- 已翻默认；回滚 `skip_low_value_refine=False`。

### 猜4 探针条带（**晋级默认**）
- 30 局 tail 最坏 6768→5862（-906s）；120 局确认 16 改善/104 平/0 恶化 → tail
  修复型修正口径晋级。`probe_plan` 蜂窝密排（k=ceil(r/20)，数学覆盖 B(c,r)
  ⊇ region）；灾难 101+ 点链 → ≤1+3k(k+1) 次 clear。回滚 `probe_strip=False`。

### 猜5 巡游 guess（**晋级默认**）
- 快筛 30/30 全优（-460s）；60 局确认 57/60（-463.7s、-9.4%）。机制：r∈
  (19.9,40] 难源提前入清除队列，TSP 巡游经过时 center clear——**与 M7
  （立即专程 clear，双程成本失败）的结构对照：巡游集成消灭专程成本**。
- c 条净小升（30 局 +89 次有界探针尝试）：按收益/风险不对称 + 链有界
  （探针 ≤19 点）+ 100% 清除 + 文档化修正晋级（修正理由随证据披露）。
- 回滚 `tour_guess=False`。

### 猜6 tour-aware 排序（失败归档）
- 7/12 差——滚动重排序噪声覆盖"远侧源认证拉长巡游"的边际收益。
- 归因：M9 近邻序已在滚动中不断重排，诱导边际的近似 key 与真实 TSP 增量
  相关性弱；全排列精确版在 pending≤8 时或可复活（开放问题）。

### 猜2（R 在线辨识）/ 猜3（ℓ-判据）/ 猜7（CMA-ES）（未实施，转队列）
- 时间预算内的取舍：猜5 的 -460s 远超猜2/3 自查的理论池（合计 ~50-150s/局）；
  猜7 需仿真在环框架搭建（新基建）。三者完整计划入开放问题。

## 3. 官方演练确认记录
- 猜1' 改变测量矩阵（跳测）：simlite 门1 全过 + 规则前提（a/b）成立——
  **需 2-3 局官方演练确认评分器接受缩减测量矩阵**（晨间清单第 1 项）。
- 猜4/5 不改变测量矩阵形状（仅清除路径与保底效率）：simlite 100% 清除
  已覆盖，官方确认并入晨间清单常规对账。

## 4. 红队战报
- exp5_redteam.py（贴边/R=1000/聚集混合摆布）：收尾轮 10 局**100% 通过**
  （完整 20 局轮5 亦 100%）。

## 5. 组合大样本（100 局 vs 晨线）
- **100/100 全优、中位 -3076.7s（-40.4%）**、最坏恶化 -381.0s（零恶化局）；
  fallback 162→0；动作数 331→161.5；中位总虚拟 **4468.5s** ≈ **338s/源
  （simlite，N 混合）**；官方换算（×1.043 漂移）≈ **353s/源**——
  **进入目标区间 330-360**。
- N 档（组合口径，近似换算）：10 源 ~370 / 13 源 ~330 / 16 源 ~295（地板
  396/340/304——16 源已达地板下方需 H 验；10/13 档接近地板）。

## 6. 最终配置与回滚（各一行）
- 入口：run_drill/sim-cli → Q3BatchPolicy；Q3Policy 默认 pure_ring8 +
  interleaved + A3 + 8000 + **skip_low_value_refine=True（本轮）**；
  Q3BatchPolicy 默认 **tspn_clear=True + probe_strip=True（本轮）+
  tour_guess=True（本轮）** + 近邻序 locate（M9）。
- 回滚：`skip_low_value_refine=False` / `probe_strip=False` /
  `tour_guess=False` / `tspn_clear=False` / 排序改回 `sorted(channel)` /
  入口换 Q3Policy。

## 7. 开放问题
1. 晨间优先：官方演练 ≥2 局（确认跳测缩减测量矩阵被接受 + 常规对账
   cleared==官方 且 c+a==20）；若异常按回滚命令逐个回退（先关 tour_guess，
   再 probe_strip，再 skip_low_value_refine——按晋级逆序隔离）。
2. 猜2/3/7 完整计划：R 辨识（指内方位源楔形远端 = ball(R)，2-3 探针读 R →
   区域按真 R 构造，sliver -17-33%）；ℓ-判据腿部（D2 的移动最小化修正；
   需与猜5 巡游集成）；CMA-ES 仿真在环（驻留点布局参数向量 + simlite 目标）。
3. 16 源档 simlite 295 < 地板 304 的口径检查（N=16 时扫描相对均摊更低，
   且 BHH 是期望——个案可低于期望，300 局级确认后写论文口径说明）。