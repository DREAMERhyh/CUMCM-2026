结论：当前代码是“离线验证通过的算法原型”，还不是可以直接参加 Q3/Q4 正式测试并提交的最终版本。刚刚复跑了全部测试：**51 项通过，耗时 52.252 秒**。Q1 可以视为完成；Q2 有一个题意级缺口；Q3/Q4 主要缺正式运行与记录层。

我核对了 B 题题面 B题.pdf、模拟器使用说明 附件1.docx 和接口指南 附件2.docx。

## 必须补的代码

1. **Q2 真正的“第二检测点候选区域”**

当前 [plot.py (line 20)](C:/Users/30130/Desktop/数模/problem_b/code/q2/plot.py:20) 中浅蓝色区域是“干扰源可能位置区域”，第二检测点只画了45个离散点，并不是题目要求的第二检测点候选区域。

建议增加并绘制：

- 保证接收区域：对所有可能源位置都不超过1000米的第二测点区域；
- 可能接收区域：在部分可能源位置、部分接收半径条件下可能测到信号的区域；
- 在保证接收区域中采样离散优化点，并明确区分“连续候选域”和“数值搜索点”。

另外，[planner.py (line 57)](C:/Users/30130/Desktop/数模/problem_b/code/q2/planner.py:57) 的“最坏后验半径”只检查有限代表点和三个误差端点，应改名为“有限场景最坏后验半径”，并增加批量合成案例，与固定侧移、FIM方法比较。

1. **Q3/Q4 正式运行入口**

目前 [q3/cli.py (line 1)](C:/Users/30130/Desktop/数模/problem_b/code/q3/cli.py:1) 和 [q4/cli.py (line 1)](C:/Users/30130/Desktop/数模/problem_b/code/q4/cli.py:1) 都只能运行伪模拟器。

需要新增有防误触机制的正式入口，例如要求明确提供：

- `--live`；
- `--robot-id`、`--base-url`；
- `--question q3/q4`；
- `--mode practice/formal`；
- 手工录入测试案例编码；
- 二次确认后才允许调用 `/enter`。

1. **现实时间截止控制**

当前 [runner.py (line 6)](C:/Users/30130/Desktop/数模/problem_b/code/runtime/runner.py:6) 没有读取 `/enter` 返回的 `remaining_real_duration_s`，只依靠动作数量上限。

必须增加：

- 使用单调时钟记录程序运行时间；
- 按实际 `remaining_real_duration_s` 建立截止时间；
- 为 `/exit` 预留约10至30秒；
- 到达软截止后停止生成新动作并主动退出；
- 异常时尽可能安全退出。

不能固定假定总有1200秒。

1. **逐动作持久化日志和表1统计**

当前运行器把日志放在内存列表中，直到正常结束后才由CLI整体写JSON。程序崩溃、断网或超时会丢失记录。

需要改成逐动作写入JSONL并立即刷新，记录：

- 请求路径、请求体、`request_id`；
- HTTP状态、响应JSON；
- 现实时间戳、单次RPC耗时；
- 最新虚拟时间；
- 当前策略阶段和退出原因。

[RunSummary (line 62)](C:/Users/30130/Desktop/数模/problem_b/code/common/models.py:62) 还需增加：

- 测试案例编码；
- 程序运行时间；
- 清除数量；
- 定位清除总时间；
- 平均定位清除时间；
- 演练结束后手工补录源总数并计算清除比例。

正式测试不显示源总数，但论文表1仍需填写案例编码、清除数量、平均定位清除时间和程序运行时间。

1. **补正式协议 testbench**

现有HTTP负载构造基本正确，重试也保持同一请求内容，但还没有真正测试HTTP通信。应增加本地HTTP桩服务器，验证：

- 200且`accepted=false`；
- 400、409、413、415、429、500；
- 超时、连接直接关闭、非法JSON；
- 同ID同内容重试不重复执行；
- 同ID不同内容返回冲突；
- 不同动作绝不并发；
- 截止前自动调用`/exit`；
- 中途异常仍保留已写日志。

1. **扩大Q3/Q4离线场景**

当前Q3、Q4状态机测试都只有一个源，不能代表题设的10至16个源。

至少增加：

- 10、13、16源；
- 接收半径1000、1250、1500米；
- 边界源、聚集源、频道1和频道20；
- Q4全向/定向混合、背向、覆盖边界±90°；
- 重复位置误差固定；
- 无真值泄漏检查；
- 动作数、虚拟时间和日志大小上限。

尤其Q4现有演示约2478个动作，必须先测真实RPC吞吐和加密日志大小，再决定是否优化121点全局网格。

## 代码之外的硬性提交项

Q3和Q4各需要：

- 充分演练后进行3次正式测试，共6次；
- 导出6份模拟器加密日志，**不得修改文件名**；
- 论文表1填写每次案例编码、清除数量、平均定位清除时间、程序运行时间；
- 每份加密日志不超过2MB。

时间很紧：题面建议在 **2026年9月13日15:30前**完成正式测试，**17:30后不能启动新测试**。

依据组委会[2026年论文格式规范](https://www.mcm.edu.cn/upload_cn/node/775/cQMeL0YY905244c8bd4b9af832f1699446d8385e.pdf)，还要保证：

- 正文不设目录且不超过30页；
- 支撑材料包含完整可运行代码和必要日志；
- 论文附录列出支撑材料并包含源程序；
- 支撑材料压缩包不超过20MB；
- 论文及材料中不得出现队员、学校等身份信息；
- 代码运行结果必须与论文一致。

Q1几何内核、共享数据模型、虚拟时间模型和Q3/Q4状态机架构可以保留，不需要推倒重写。最新缺口已经同步到 [PROJECT_CONTEXT.md (line 45)](C:/Users/30130/Desktop/数模/problem_b/PROJECT_CONTEXT.md:45)。

- 补全Q2候选域
- 实现正式运行层
- 扩充离线测试